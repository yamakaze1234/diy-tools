# CloudBase 多人同步实施方案

日期：2026-09-16。状态：已开始实施。用户确认沿用现有 PostgreSQL 环境；接入及配置/共享成本最小同步已落地并用合成数据验证。阶段 A 的素材/小程序验证、阶段 C/D 仍未完成。当前证据与操作入口见 `prototype/多人同步使用与验收.md`。

## 1. 目标与技术选择

适用范围：4～5 名内部成员，电脑继续本地运行，后续接微信小程序临时维护；成员业务权限相同。

第一版采用 CloudBase 中国内地环境、Node.js 事件云函数、PostgreSQL、身份认证和云存储。按成本/配置等业务记录保存，使用服务端事务完成并发提交。保留独立的存储适配层，业务计算、差异合并不依赖 CloudBase SDK。

图片范围已于 2026-09-16 调整：生成的配置清单图、SKU 图和导出 ZIP 只在本地生成/保存，不上传云端；云端保存配置内容、样式设置、图库分类和原始素材。工作台增加独立的多级机箱图库，安排专人维护。

2026-09-16 技术决定：现有环境只提供 PostgreSQL，用户已确认沿用。嵌套配置保存为 JSONB；`wb_api` 数据库函数在单事务内校验成员、锁定工作区序号、比较版本、合并并写入记录/变更/回执。表默认拒绝应用账号直接读写，通过带身份校验的 RPC 访问。逻辑记录类型保持原方案，暂不引入常驻容器或实时协同编辑服务。

## 2. 已核对的项目事实

- `prototype/server.mjs`：以 `.local/state.json` 保存工作数据，启动时读入内存；`/api/state` 用整份 `revision` 检查本机页面并发，通过队列串行写文件。
- `prototype/app.js`：编辑后约 950 毫秒触发本地保存；现有冲突界面是下载草稿和重新载入。操作人来自可编辑名称，不能当作云端登录身份。
- `prototype/shops.js`：当前数据版本为 3，含英特尔、技嘉、乔思伯；配置、模板、输出源、优惠券按店铺区分。
- `prototype/erp-sync.js`：成本源用 goodsId 匹配；ERP 账号/仓库范围已有校验；ERP 更新与人工含税核算价分开，采集不完整时保留旧数据。
- `三店扩展与图片模板设计方案.md` 的共享成本要求，与当前代码的共享成本结构一致。早期实施计划中的按店成本表述不能据此改成三份独立人工成本。

## 3. 整体架构

```text
电脑浏览器界面 → 本机 Node 服务 → 本地 SQLite：缓存、草稿、待提交队列
                         ↓ 用户登录态调用
                  CloudBase workbenchApi 云函数
                         ↑ 微信原生云调用
                      微信小程序

workbenchApi → PostgreSQL wb_api 事务 → JSONB 业务记录 / 私有云存储（待接入）
```

两种入口共用业务处理函数，入口适配器分别校验身份，统一映射为内部 memberId。电脑端使用 CloudBase 用户访问令牌调用事件云函数 HTTP API；小程序调用同一事件云函数。不要混用事件函数与 HTTP 函数的入参格式。

云端保存大家已经提交成功的正式数据。本地保存最后确认的云端版本、尚未提交的编辑和待处理冲突。收到云端数据时，不覆盖正在编辑的草稿。

本地引入 SQLite 的目的，是将本地编辑和待提交任务在一个事务中保存，避免程序中断后出现“数据保存了，但同步任务丢失”。现有 JSON 保留为迁移输入及导入导出格式，不将工作数据库放入坚果云同步目录。

## 4. 登录：同权成员，识别身份

### 电脑端

为成员开通 CloudBase 用户名/邮箱密码登录，优先使用当前套餐支持的用户名密码方式。登录由官方 SDK 管理，业务服务不保存明文密码。

浏览器登录完成后向本机服务建立受来源校验保护的本地会话，本机同步器使用用户令牌访问云端。需要跨重启保留的凭证存入 Windows 凭据存储；日志、JSON 导出和共享目录不包含凭证。环境 ID 是配置项，不是登录凭证。

### 小程序

用微信云开发受信任调用上下文中的 APPID/OPENID 识别调用者。不能接受客户端自行填写的 openId、memberId 或操作人名称作为授权依据。

首次在已经登录的电脑端生成短期、一次性的绑定码，小程序输入并由电脑端确认绑定到当前 memberId；绑定记录按事务消费，防止重复绑定。不能假设电脑 UID 与微信 OPENID 自动属于同一人，也不依赖一定存在 UNIONID。

### 成员边界

所有成员有相同业务能力，不开发角色或审批系统；只允许已加入工作组的身份访问该工作区。成员初始化在控制台/初始化脚本完成，第一版不做复杂管理界面。客户端不持有腾讯云 SecretId/SecretKey，数据库不开放匿名读写。

账号功能和所选套餐适配作为接入验证项；不因套餐名称包含“个人”或“团队”直接推断能否供 5 人使用。

## 5. 逻辑记录类型（PostgreSQL 存储）

| 集合 | 主键/范围 | 内容 |
|---|---|---|
| workspaces | workspaceId | 数据格式版本、当前已提交序号、当前基础快照与 ERP 快照指针 |
| members / identities | memberId / 身份来源唯一键 | 名称、成员状态、电脑 UID 与微信身份映射 |
| shops | workspaceId + shopId | 店铺信息、优惠券及品牌设置 |
| components | workspaceId + erpScopeId + goodsId | 人工含税单价、原始名称及人工维护信息 |
| source_entries | workspaceId + sourceId | shopId、组件引用、展示名称、升级/质保/加购文案 |
| products | workspaceId + productId | shopId、商品链接、SPU、分类等链接级信息 |
| configurations | workspaceId + configId | shopId、productId、SKU、配件组合、数量、售价、分期、图片布局 |
| templates | workspaceId + templateId | 店铺模板和样式；记录模板结构版本 |
| erp_imports / erp_rows | importId / importId + goodsId | ERP 来源、采集任务、完整性信息、成本及库存快照 |
| changes / mutations | workspaceId + seq / workspaceId + mutationId | 增量变更、处理结果、请求哈希与重试去重信息 |
| assets | workspaceId + 内容哈希 | 云存储 fileId、类型、大小、引用、最后解除引用时间、回收时间与素材状态 |
| case_brands / case_models / case_variants | 工作区内稳定 ID | 品牌、型号、颜色和图片版本分类，支持排序、改名、停用；原始机箱素材三店共享 |
| case_images / case_image_versions | imageId / imageId + revision | 图库条目、分类、角度/用途、不可变素材版本、维护人、可用状态、适用 ERP 配件映射 |

当前物理表为 `wb_workspaces`、`wb_members`、`wb_records`、`wb_changes`、`wb_mutations`。配置和成本通过 `entity_type` 区分；其余逻辑类型在阶段 C 接入。逻辑复合键对应确定性记录 ID，并建立必要索引。所有查询都带工作区范围；店铺业务查询另带 shopId。

可编辑记录包含 `version`、`updatedAt`、`updatedBy`、`deletedAt`。时间由服务端产生；并发判断用版本，不用电脑时钟。ERP/商品/SPU/SKU 等外部 ID 始终用字符串。

金额存整数分；数量与精度规则沿用项目核算契约。迁移时验证小数转换与既有利润计算结果，不能因上云改变舍入方式。缺成本与零成本分开存储。

配置配件行新增稳定 lineId，避免数组顺序变化后按下标错误合并。未绑定真实 goodsId 的行保留原文和待绑定状态，不按名称猜测共享成本。

成本和库存以共享来源为准；配置内旧有重复价格迁移为缓存/派生值。人工单价存 components，ERP 成本库存从当前 ERP 快照解析。修改共享成本不批量改写所有配置，仅使有关计算缓存失效。

## 6. 同步协议

### 本地落盘与发送

1. 编辑保持现有近一秒本地自动保存体验。
2. 在一个本地事务里写入草稿和待提交操作；记录该次编辑的基础版本和基础字段值。
3. 云端提交在停止编辑约 2～3 秒后触发；同一设备的发送器串行工作。已经发送的操作保持原样，后续编辑形成新的操作。
4. 云端确认后更新本地基础版本、移除对应任务，再把发送期间新增的草稿合并到新基础上；不能直接用响应覆盖新输入。
5. 启动、回到前台、点击同步时立即检查；前台默认每 30 秒检查一次增量，后台降低频率，失败退避。

提交是业务操作，例如“调整某成本”“修改某配置”“删除某模板”，不提交整份 state.json。批量动作保留预览和影响范围，开始发送后再按小批次处理。

### 原子提交与可重试

一次小事务完成：读取 mutationId 去重记录 → 检查成员及目标版本 → 校验/合并 → 更新记录 → 分配工作区提交序号 → 写 changes 与 mutation 结果。

相同 mutationId、相同内容重试返回原处理结果；相同 ID 携带不同内容拒绝。网络超时保持“结果待确认”，按原 ID 查询/重试，不能假定失败后生成新 ID 再提交。

工作区序号在同一数据库事务中递增，使 changes 有完整的已提交顺序。所有提交只在云函数中产生，不能用云函数进程内的变量或队列作为多实例并发锁。

### 增量拉取与首次加载

服务端返回固定上界 headSeq 内的分页变化及 nextCursor；变化携带提交后的记录内容或删除标记。每页在本地事务中应用并推进游标，崩溃后可重复读取。

首次加载采用已发布的不可变基础快照，加快照序号之后的增量。快照生成从既有快照重放日志到固定序号，不把多页查询当前表误当成某个时间点的一致快照。快照清单包含哈希、记录数、数据格式版本和 lastSeq。

日志拟保留至少 90 天；实际归档按最后有效快照与恢复需求执行。离线游标过旧时返回重新初始化标志，本地先保留草稿及其基础值，再取新快照并做三方比较。

状态显示：本地已保存、待同步 N 项、正在同步、已与云端同步、存在冲突、离线、结果待确认。“已同步”表示当前提交获得云端确认，不表示其他设备都已收到。

## 7. 合并与冲突

比较共同基础值 B、本地提交 L、当前云端值 R：L=B 采用 R；R=B 采用 L；L=R 直接一致；三者不同则需要处理。

| 情形 | 默认行为 |
|---|---|
| 改不同记录 | 分别提交 |
| 改同一配置中相互独立的字段 | 校验通过自动合并 |
| 同字段改成相同值 | 不产生重复修改 |
| 同一含税单价改成不同值 | 冲突弹窗 |
| 更换配件与修改该行数量并发 | 按配件关联组处理，不能机械拼接 |
| 删除与编辑并发 | 冲突弹窗 |
| 两人创建同一来源的 goodsId | 由确定性主键识别重复，检查字段差异 |
| ERP 更新与人工含税价修改 | 写入各自来源，按最新输入重新核算 |

支持字段的“缺省继承”“明确清空”“删除”三种语义，不将空值统一解释为删除。跨店展示文案不互相覆盖。

冲突弹窗展示店铺/共享范围、记录名与真实 ID、字段名、基础值、我的值、云端值、修改人/时间、成本变更影响的配置数；支持采用我的、采用云端、手动填写、稍后处理。

冲突选择提交时携带刚看到的云端版本，仍执行原子版本检查。若其他人再次修改，则重新比较。只阻塞有冲突的记录/关联业务组，其他独立记录继续同步。

撤销未提交草稿只影响本机；撤销已同步修改要生成新的有版本检查的补偿操作，不能恢复旧整表。删除采用 tombstone，不因重启隐藏恢复入口；恢复也是新提交。

## 8. ERP 与大批量导入

ERP 浏览器采集继续在本机运行，保留可见账号、仓库、分页完整性和真实 goodsId 校验。会话、Cookie、采集令牌不上传云端。

ERP 上传流程：申请 importId 与发布基准 → 本地采集 → 分块上传到不可变暂存快照 → 云端检查数量、重复 ID、来源、分块哈希和清单 → 短事务发布当前快照指针。

全部分块验证通过前，其他设备继续读取旧快照。指针发布使用版本检查；并发采集按服务端登记的任务序列裁决，较旧任务不能在较新任务发布后自动覆盖它。失败保留旧有效快照和旧成功时间。

所有读取固定一次快照 ID，避免翻页过程中跨快照。每台设备完整下载所需快照数据、校验并落盘后再切换本地指针；同步中显示旧快照及其时间，不能宣称已经拿到最新库存。

人工成本批量导入分业务小批次提交，显示已成功、冲突、失败、待处理条数，并允许按 mutationId 恢复；不把整个批次描述为一次原子事务。ERP 原子发布与人工成本分批提交是两种不同策略。

PostgreSQL 版目前每次事务提交一条业务记录；工作区行锁串行分配变更序号，并在同一事务写入记录、变更和幂等回执。批量操作尚未接入，后续需单独设定体积/时长预算；事务内不上传文件或请求外部接口。原文档数据库的 100 操作限制不适用于此实现。

## 9. 素材和配置图

- **云数据库保存**：配置内容、配件、数量、文字、样式、模块顺序、显示开关、长图/方图各自的图片位置和大小、图库分类及素材引用。
- **云存储保存**：机箱原图，以及配置确实依赖的 Logo、自定义原始素材和备份。原始文件按内容哈希去重；同一图片被多套配置引用不重复上传。
- **本地生成与保存**：配置清单图、1:1 SKU 图、生成图预览、导出 ZIP、图库缩略图和素材缓存。生成图不上传云存储，云端备份也不打包这些可重建的输出文件。
- 数据记录存稳定 imageId、imageRevision 和 assetId/fileId，不存会过期的临时下载 URL或某台电脑的路径。文件上传并核验后才发布素材版本和配置引用；失败保留草稿。
- 本地预览缓存关联配置、样式、素材、渲染器及实际影响画面的其他数据版本；输入变化后在本机重绘。新电脑下载配置和原始素材即可重新生成，无须取回旧 PNG。
- 小程序第一版显示配置明细、样式信息与机箱原图，不再依赖云端成品图；以后需要完整配置图预览/导出时，增加手机端 Canvas 渲染能力。手机修改成本不以生成图片为前提。
- 离线可用已经缓存的素材；未缓存时提示素材待下载，禁止将缺图结果当成完整图片成功导出。

### 9.1 独立机箱图库

本地模块进展（2026-09-16）：已实现侧栏机箱图库、右上角操作记录、多级分类、搜索、新增、编辑资料、删除入回收站和恢复；“更换机箱图”可搜索选用现有素材，保留配置的图片变换与样式。当前使用本地 state.json 和素材目录，云端接口、素材版本发布及批量更新引用仍按后文方案待实施。

工作台增加独立的“机箱图库”入口。图库是工作区共享资源，三店选择相同机箱时引用同一素材；店铺配色和图上排版仍属于各自配置。

采用固定业务层级：品牌 → 型号 → 颜色 → 图片版本 → 图片。示例中的“黑白”默认展开为黑色、白色两个可选项；若实物是黑白双色，可单独创建“黑白双色”，不强制拆成两种颜色。

```text
机箱图库
└─ 乔思伯
   └─ X400CG
      ├─ 黑色
      │  ├─ 进阶
      │  │  ├─ 正面主图
      │  │  └─ 侧面图
      │  └─ 豪华
      │     └─ 主图
      └─ 白色
         ├─ 进阶
         └─ 豪华
```

“进阶/豪华”是图库版本标签，用于区分图片中的风扇、灯效或装配外观，由维护人明确填写；不自动等同于整机配置中的“进阶版/豪华版”。某型号只有一种图片时可设“通用”版本；同一原图可以复用，不需要复制文件填满分类。

界面分区：左侧分类树，中间图片卡片，右侧详情与维护区。点击任一级可查看该分支全部图片，支持展开/收起；搜索可按品牌、型号、颜色、版本或准确 ERP ID 定位，并显示完整分类路径。

卡片显示缩略图、型号/颜色/版本、主图标记、使用配置数量、维护人和更新时间。详情区提供大图、适用配件、图片尺寸、版本历史与引用该图片的配置列表；“待分类”“已停用”“回收站”单独展示。

维护流程：批量上传 → 分配品牌/型号/颜色/版本 → 选择主图及角度 → 预览确认 → 发布。新增原图先按哈希去重，文件名仅用于辅助填分类，不据此猜测真实 goodsId。批量上传时可统一指定公共分类，再逐图补颜色或版本。

分类使用稳定 ID，显示名称不是引用键。维护人可以改名、排序和移动条目，现有配置引用不因此失效。存在条目的分类须先迁移条目才能删除；停用图库条目只影响新的选图，不使老配置缺图。

“专人维护”落实为成员分工及维护人记录，沿用全员同权的决定，不自动增加角色权限或审批流程。

### 9.2 配置如何使用图库

配置编辑中的“选择机箱图”打开图库选择器，可从当前机箱的已确认配件映射筛选，再按颜色、图片版本和角度选择。映射使用 `erpScopeId + goodsId`，不按型号相似程度自动绑定；没有唯一结果时由用户选择。

配置保存 `caseImageRef = { imageId, revision }`；每个素材版本引用不可变 assetId。原有 `caseTransforms.long`、`caseTransforms.square` 和显示开关继续属于配置，可分别调整。

- 更换 CPU、内存等非机箱配件：保留该配置选中的机箱图及位置、大小、字体和颜色。
- 更换机箱配件：预览中列出配件变化和图片变化；优先推荐准确映射的候选图，支持选择新图或明确保留原图。旧图与新配件不匹配时提示，不能静默当成已匹配。
- 新图沿用原配置的位置和大小，预览发现比例/留白不合适时调整；可提供“恢复默认位置”，不能因为换配件就覆盖整套样式。
- 跨店复制配置可复用同一原图引用，店铺品牌和排版仍采用目标店规则。

### 9.3 维护人更新原图和批量换图

替换原图创建新素材版本，不覆盖旧文件。老配置默认固定原版本，避免维护人换一张图就让所有老配置发生未预览的变化。

发布新版本后显示引用情况，可选择“仅供以后选用”或“同步更新已有配置”。后者先按店铺/链接/配置展示影响清单，勾选后逐配置提交带基础版本的图片引用修改；保持各配置原有样式和图片位置，冲突项单独处理。

同一图片跨品牌/颜色分类的语义变更，与“修正同一素材的新版本”分开处理，不能以升级图片的方式把黑色机箱静默变为白色。支持从旧版本回退，仍作为一次可追溯的新提交。

### 9.4 原始素材定期清理

用户要求图片每 1～2 个月清理一次。调整后云端没有生成的配置图，清理仅针对无保留用途的原始素材。默认每 60 天执行一次，可改为每 30 天；本地生成图不在该云端任务范围内。

- **保留范围**：已发布且可选用的图库图片，即使暂时没有配置引用也保留；当前配置、模板、店铺品牌、尚在恢复期的删除记录及有效备份依赖的图片保留。同一素材的所有图库和配置引用统一核对，不按上传时间直接删除。
- **候选范围**：没有图库条目引用的遗留上传、明确停用且不再保留的素材版本，在没有配置/备份等有效引用且连续闲置至少 30 天后进入候选清单。图库草稿和上传中的文件不参与自动删除；维护人明确放弃草稿后才解除其保留用途。
- **先回收**：清理任务记录候选文件、大小、原因及预计释放空间，将符合条件的图片标记为回收状态，默认保留 7 天，可恢复。回收阶段不宣称已经释放存储空间。
- **到期清除**：回收期到期后再次检查引用，再按已配置的清理策略永久删除对象并记录结果；失败可重试，已恢复或重新使用的图片不删除。界面明确展示回收期和到期永久删除规则。
- **并发保护**：图片引用新增与素材状态变更通过事务协调；已经进入最终删除状态的素材不能再绑定，需重新上传。云存储删除与数据库事务无法合并，采用任务状态和重复执行保护，成功删除后才统计实际释放空间。
- **备份保护**：备份清单引用的素材在对应备份有效期内保留，或备份包本身包含图片实体；不能只留 JSON 引用却删掉唯一素材，造成备份无法恢复。
- **执行方式**：拟由每日定时任务检查上次完整清理时间，达到 30/60 天周期才开展候选扫描，并处理已到期回收项。每次记录扫描、回收、保留、删除失败和实际释放空间，支持手动查看清单及执行同一策略。

这是工作台计划实现的回收机制，不假设 CloudBase 云存储自带回收站。正式环境若无法实现回收，必须先提示用户，不能静默改成直接永久删除。本次仅更新方案，没有删除任何本地或云端文件。

## 10. 业务接口（应用协议，非厂商固定 API）

统一事件结构 `{action, requestId, payload}`，处理人和工作区由已验证身份确定。CloudBase HTTP 调用与小程序云调用转换到相同业务响应格式。

| action | 用途 |
|---|---|
| session.get | 成员身份、工作区、数据格式及最低客户端版本 |
| sync.bootstrap | 基础快照清单、固定同步上界 |
| sync.pull | 按游标分页读取已提交变更 |
| sync.push | 提交有基础版本的业务修改，返回成功或结构化冲突 |
| sync.receipt | 查询 mutationId 处理结果 |
| assets.prepare / assets.complete | 申请上传、确认素材可引用 |
| gallery.list / gallery.detail | 多级分类、图片列表、素材版本及引用情况 |
| gallery.save / gallery.publish | 分类维护、条目编辑和不可变素材版本发布 |
| gallery.usage / gallery.apply | 预览受影响配置，按选择批量更新图片引用 |
| erp.begin / erp.upload / erp.finalize | 采集任务、分块上传、校验及发布 |
| identity.bindStart / bindConfirm | 电脑成员与小程序身份一次性绑定 |
| records.history | 查询单记录历史及变更来源 |

核心冲突返回 `code=CONFLICT` 与结构化字段，HTTP 外层状态与云函数业务状态分别处理。明确区分未登录、协议版本过旧、格式错误、冲突、瞬时故障，不能一律当成可重试网络错误。

提交示意：

```json
{
  "action": "sync.push",
  "requestId": "request-uuid",
  "payload": {
    "mutationId": "mutation-uuid",
    "deviceId": "device-uuid",
    "entityType": "component",
    "entityId": "stable-component-id",
    "baseVersion": 12,
    "changes": [{"field": "taxCents", "before": 50000, "after": 52000}]
  }
}
```

服务端不根据客户端 before 值判断权限；需要历史的恢复操作核对保存的历史版本。原子事务最终检查最新版本并作业务校验。协议版本独立于当前本机 dataVersion=3，旧客户端不能静默覆盖新增字段。

## 11. 文件改造范围

建议增设：

```text
shared/domain/              数据校验、核算、记录模型
shared/sync/                三方合并、变更协议、格式迁移
cloud/functions/workbenchApi/  身份适配与业务入口
cloud/repositories/         CloudBase 文档库适配
cloud/jobs/                 备份、快照、导入校验、图片定期清理
prototype/local-store/      SQLite、待提交队列、凭证适配
prototype/sync-client.js    提交、拉取、重试与游标
prototype/sync-ui.js        状态、冲突对比、同步历史
prototype/case-gallery.js   多级机箱图库、素材维护、配置选图
miniprogram/               第二阶段新增手机端
```

调整 server.mjs 的本地持久化层，保留本机监听和 ERP 桥接；app.js 接入草稿/同步状态，拆开“本地保存”和“云端提交”。保留 core、pricing、poster 等现有行为，抽取共用逻辑时用现有业务测试确认结果一致。

## 12. 初始迁移、备份与恢复

1. 选定一台电脑的当前数据作为首次基线，备份 state.json、素材和程序版本；不从多台电脑同时初始化云端。
2. 只读生成迁移预览，列出三店配置、商品、输出源、成本、模板数量，以及缺失 ID、来源冲突、图片缺失和重复配件价格差异。原有 caseImage 对应原图按哈希去重导入图库并保留引用；分类未知先进入待分类，不能按文件名猜测品牌/颜色。已有生成 PNG/ZIP 不上传，也不删除本地原文件。
3. 人工含税价以确认后的共享成本源为准；旧配置缓存与其不同的条目单独列出，不能未经核对直接丢弃差异。
4. 确定已验证 ERP 来源；历史成本无法确定来源时标为待核对，禁止猜测账套。
5. 在暂存区域分批导入，验证记录数、内容哈希、长 ID、核算结果及素材引用，然后一次发布工作区基线。
6. 第二台电脑首次接入先保存本机数据。已有独立修改时导入为待合并草稿，不能直接全量替换云端。
7. 每日生成一致快照和素材清单，建议保留 30 天；定期下载一份离云备份，可放坚果云。备份成功须有清单与哈希，并验证实际恢复。
8. 云端已有多人新数据后，回退客户端版本不能把旧 JSON 全量覆盖云端；应在兼容模式使用或先导出并恢复到独立测试工作区。

## 13. 实施顺序与完成标准

### 阶段 A：接入验证

使用合成数据验证内地环境、套餐支持的登录方式、电脑用户令牌调用、小程序调用上下文、数据库事务、素材上传和一次恢复。记录真实调用资源消耗与延迟。此阶段不迁移正式数据。

### 阶段 B：两台电脑最小同步

完成稳定记录 ID、独立同步协议、本地 SQLite/outbox；先打通配置和人工成本的提交、拉取、断网重试，再完成自动合并与冲突弹窗。

### 阶段 C：正式业务接入

接入三店展示边界、ERP 快照发布、多级机箱图库、不可变素材版本、配置绑定与批量换图、30/60 天原始素材清理及回收机制、迁移预览、历史及备份恢复。生成配置图只保存在本地。完成受控迁移后扩展到全组。

### 阶段 D：微信小程序

新增身份绑定、店铺切换、成本搜索、成本/售价编辑、配置明细与机箱原图查看和冲突处理。第一版大批量导入、图库集中维护、ERP 采集和配置成品图渲染继续由电脑端承担；后续手机完整预览使用端侧渲染，不增加云端成品图存储。手机编辑成本显示其共享范围和三店影响。

### 必要验收

- 两台电脑修改不同记录，都保留；同一成本不同值必出冲突。
- 同一配置独立字段可合并；配件更换与数量并发不产生错误组合。
- 云端成功但响应丢失，重复提交只有一次有效变更。
- 保存后杀进程、离线编辑并重启，草稿和待提交队列仍在。
- 同步响应到达时仍在输入，不覆盖新输入；撤销不还原他人的整表。
- 分页拉取期间发生新增、更新、删除，不漏数据且断点可恢复。
- 旧客户端被明确提示升级；旧离线游标重新加载时保留草稿。
- ERP 分块失败不发布；并发旧采集不会覆盖新快照；人工核算价保持。
- 技嘉展示文字修改不影响另外两店；共享成本更新影响三店引用者。
- 删除与编辑、两人同时解决冲突，均再次检查版本。
- 在空白测试环境恢复备份，核对记录、金额、长 ID 和素材。
- 云存储和云端备份均无生成配置图/导出 ZIP；新电脑下载内容、样式和原图后能重新生成。
- 图库按品牌/型号/颜色/图片版本多级浏览，重命名与移动分类不破坏旧配置引用。
- 原图升级保留老配置的固定版本；批量换图保留各配置样式与图片位置，并检测并发冲突。
- 素材清理保留可选用但零引用的图库图片、在用旧版本和有效备份依赖；回收、恢复、并发引用及失败重试不会造成误删或错误统计空间。
- 电脑与手机绑定为同一成员；未加入工作组的身份不能读写。

## 14. 资源与待落实信息

初始只需一个用于验证的 CloudBase 内地环境。正式使用前准备小程序 AppID 及环境关联、4～5 个成员登录身份、当前正式数据基线、云端费用上限。开发/正式环境可分开，按实际预算选择；正式数据不用于破坏性测试。

本方案不承诺某个低价套餐一定支持全部功能，也不根据人数估算精确账单。先核对所需认证、事务、存储与函数配额，再用实际请求量定档。第一版不需要 AI 模型或 Token 套餐能力。

## 15. 本次核对的官方资料

- 文档数据库事务：https://docs.cloudbase.net/database/transaction
- CloudBase 身份认证：https://docs.cloudbase.net/authentication/auth/introduce
- 身份认证 V3（含服务端调用者上下文）：https://docs.cloudbase.net/api-reference/webv3/authentication
- 云函数 HTTP API：https://docs.cloudbase.net/http-api/functions/%E4%BA%91%E5%87%BD%E6%95%B0
- 当前用户信息 HTTP 接口：https://docs.cloudbase.net/http-api/auth/user-me
- HTTP 云函数调用方式（用于区分函数类型）：https://docs.cloudbase.net/cloud-function/function-calls/

实际实施时固定经过验证的 SDK 版本及函数入口类型，不混用旧版和新版认证示例；本方案中的 action 名为项目拟定协议。


## 2026-09-16 电脑端实测交付更新

电脑端正式同步与 Windows EXE 的当前实现、证据及未完成项，以 `prototype/多人同步使用与验收.md` 为准。业务使用 PostgreSQL 协议 2（wa_*），旧验证协议 1（wb_*）保留独立。正式工作区 workbench-main 由用户在界面预览后首次发布。个人版已升级生效。公司 SQL 和脚本为并存来源，SQL 为公司内主要方式，脚本保留供外出使用；SQL 实际连接待提供数据库信息。小程序、素材周期清理和云端定期备份不能据电脑端完成而认定完成。
