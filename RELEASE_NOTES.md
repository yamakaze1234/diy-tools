# 1.0.3

本版将桌面工作台升级到 1.0.3，重点改进配置容量、配件维护、批量工作流和图片视图一致性：

- 每个店铺支持 8,000 套在用配置；新建、模板导入、标准 JSON 导入和本机保存统一执行容量校验，已删除配置不占额度。
- 配件名称栏可直接按名称或 ERP ID 搜索并选择，修正保存回执字段顺序和连续换件误报冲突，同时保留真实并发冲突检查。
- 优化模板连续编号、加购新旧数量、图库与大数据保存、ERP 库存显示、批量换件及轻量网页版维护能力。
- 工作区保存和草稿恢复请求上限提高到 256 MB，其他请求仍保持 40 MB。
- 详情图与 SKU 图切换时统一本店配置视图和补白模式；新增配置与重新载入后继续沿用店铺设置。

Windows 分发 ZIP 内含 EXE、运行依赖、WebView2 安装程序、使用说明和 SHA256 清单。公开包不含本地 `data`、业务数据库、登录缓存、SQL 凭据、团队云配置或成员账号。

升级前先退出旧版并备份旧目录。将旧版 EXE 同级的整个 `data` 文件夹复制到新版 EXE 同级位置；不要覆盖或删除旧版目录，确认新版数据正常后再继续使用。
