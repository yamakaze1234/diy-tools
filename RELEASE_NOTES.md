# 0.3.4

修复编辑时删除配置误报“编辑期间同一字段已变化：$record”的问题。保留已保存的配置顺序，并兼容没有排序字段的旧记录；真正的并发编辑冲突仍会拦截。

升级前正常退出旧版，保留并沿用旧程序旁的整个 data 文件夹。解压新版后，将旧 data 复制到新版 EXE 同级目录再启动；旧目录保留。

公开 Windows 包使用云配置占位模板，不包含团队云配置、业务数据或登录凭据。已有部署需沿用自己的云配置。
