# 1.0.18

- 展示配件的上方名称严格显示输出源名称；自定义展示名称独立保存，只用于配置图。留空则沿用输出源名称。
- 升级时保留已修改的图片展示文字，并恢复其输出源名称；实际配置和配件 ID 不变。

# 1.0.17

- 点击配置只切换当前编辑对象，保留本店已勾选的配置；跨店切换时清除旧店勾选。
- “展示配件”新增独立的展示名称输入框；自定义名称用于配置图，保留实际配置的型号名称与原配件 ID。
- “批量修改配件加购描述”按原配件和链接筛选，只修改展示配件的逐项升级说明，保留手动添加的加购条目。

# 1.0.16

- “批量配色与格式”的“全部编辑格式”会把当前配置新增的独立图片连同素材、位置、大小和图层顺序应用到目标配置；目标独有图片保留，重复应用不产生重复图层。

# 1.0.15

- 修复配置复制后编辑名称、配件和数量时可能出现的本机保存失败：同步记录重建只读取当前草稿，并扩大规则引擎的内存余量。

# 1.0.14

- 修正“多人同步 → 还原配置 / 历史版本”点击后无反应；进入时立即显示读取状态。
- 新增“多人同步 → 本机空间 → 清理缓存”，预览旧版自动生成图片的数量和占用后移入 Windows 回收站；每套配置最新图片及业务数据保留。

# 1.0.13

- 单张 PNG 和批量 ZIP 导出立即使用点击时的配置快照，不等待全量本机保存返回。
- 保存继续在后台进行；使用尚未保存草稿导出时显示提示，批量清单不误标已保存版本号。
- 导出期间暂缓自动生成的背景图片任务，完成后继续处理。

# 1.0.12

- WPS 导入按本店输出表的精确 goods ID 选用显示名称，并导入加购描述到逐项升级说明；重复 ID 需要用输出表名称消歧。
- 输出源差异同步加入逐项升级说明，支持仅文字说明和多选加购；已有手写说明默认保留，预览中可选择覆盖。
- 升级时只补空白的来源说明，保留 620 链接和其他配置的手工文案。

# 1.0.11

- 深色与浅色按当前店铺统一切换，保留各配置自定义配色，新增配置沿用店铺选择。
- 输出源保存仅投影变化记录，减少重复差异计算与逐条规则调用。
- 保存失败保留不含业务内容和凭据的本机堆栈定位记录。
- 本机发布恢复并校验正式云端配置，分发包仍不包含业务数据。

# 1.0.10

- 提速批量新增和替换配件：减少重复配置复制与全表查找；保存时仅转换确有变化的配置，保留并发冲突、排序与删除检查。
- 批量应用立即显示保存进度，保存完成前锁定操作，避免重复提交。
- 配置总览统一表头与金额列宽，ERP 提示独立成行，分期利润分行展示，窄窗口不再挤压遮挡。
- 模拟 1,000 套配置、3,000 条输出源、16,000 条成本记录，修改 20 套配置的本机保存从 5.99 秒降至 2.01 秒；不代表真实云端同步耗时。

# 1.0.9

- SQL 未找到 ERP 单价时，配置价格暂用核算成本计算并以黄色标明“ERP价格未识别，请核算货源情况和价格”；两种成本都缺失时不计算 ERP 利润，SQL 返回 0 元仍按真实价格处理。
- 全局批量替换配件的预览同时列出展示配件和实际配置的对应变化。

# 1.0.8

- 桌面版保留账号密码自动填充；关闭并清理 WebView2 保存的其他表单输入，配置搜索不再弹出“保存的信息”。

# 1.0.7

- 历史快照使用可校验的 zlib 无损压缩；连续编辑按阶段生成恢复点，重要操作立即留档。
- 历史数据管理可显示占用、创建和删除命名版本，并预览、主动清理普通历史；每周自动清理，历史库目标不超过 5 GiB。
- 旧历史库通过离线逐条校验工具转换，保留原库作为回退；当前配置保存与图片导出沿用即时状态。
- 历史还原保留本机 ERP 库存、成本及同步状态；同字段并发修改的现有保护继续有效。

# 1.0.6

- 核算利润统一为：到手价 −（含税总成本 × 1.04＋100）− 分期费。
- ERP 利润仍为到手价 × 98% − ERP 总成本；不修改配件成本和库存。
- 同步更新界面公式提示、说明书示例和回归测试。

# 1.0.5

- 修复新建配置保存误报冲突及冲突弹窗无法重新打开。
- 编辑配置选择配件后，将加购描述带入对应逐项升级说明。
- 配置列表支持同链接拖动排序；图片模块提供拖动手柄和前后落点提示。
- 点击配置卡片编辑时重置旧勾选，批量导出显示套数并提供清空入口。
- 紧凑侧栏：店铺设置位于店铺选择旁，工具与资料、管理列表位于店铺下方。
- 清理已被现行文档替代的历史方案、验收记录和临时介绍资料。

# 1.0.4

- 修复新建配置首次保存时补齐未知库存字段导致的保存冲突，保留继续选件的输入。
- 修复进入冲突状态后再次点击保存无法打开处理弹窗的问题。
- 保留真实并发修改的冲突校验；升级时保留旧数据和恢复草稿。

# 1.0.3

本版将桌面工作台升级到 1.0.3，重点改进配置容量、配件维护、批量工作流和图片视图一致性：

- 每个店铺支持 8,000 套在用配置；新建、模板导入、标准 JSON 导入和本机保存统一执行容量校验，已删除配置不占额度。
- 配件名称栏可直接按名称或 ERP ID 搜索并选择，修正保存回执字段顺序和连续换件误报冲突，同时保留真实并发冲突检查。
- 优化模板连续编号、加购新旧数量、图库与大数据保存、ERP 库存显示、批量换件及轻量网页版维护能力。
- 工作区保存和草稿恢复请求上限提高到 256 MB，其他请求仍保持 40 MB。
- 详情图与 SKU 图切换时统一本店配置视图和补白模式；新增配置与重新载入后继续沿用店铺设置。

Windows 分发 ZIP 内含 EXE、运行依赖、WebView2 安装程序、使用说明和 SHA256 清单。公开包不含本地 `data`、业务数据库、登录缓存、SQL 凭据、团队云配置或成员账号。

升级前先退出旧版并备份旧目录。将旧版 EXE 同级的整个 `data` 文件夹复制到新版 EXE 同级位置；不要覆盖或删除旧版目录，确认新版数据正常后再继续使用。
