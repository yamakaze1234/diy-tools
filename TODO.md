# DIY 配置工作台待办清单

更新日期：2026-09-14

依据：[V1.5 实施计划](D:/工作/PythonProject/配置工具/DIY配置系统_V1.5_实施计划.md)。沿用已有任务编号，新增图文联动与维护表任务；阶段编号用于分组，执行优先级以下方主线为准。

需求与范围已整理，已收到并只读核验用户精简 XLSX，记录见[在线表格样例分析](D:/工作/PythonProject/配置工具/在线表格样例分析.md)。已完成首版本地可操作原型，完整实施任务进度仍为 **0 / 38**（原型仅覆盖各任务的部分条件）；本次完成部分 S0 样例核对，不代表整个任务完成。功能及对应验收完成后再勾选，记录完成日期和验证结果。

## 核心目标

勾选共享模板中的现成配置 → 在线补充/编辑配置 → 配置清单图即时预览 → 保存后自动生成并保存新图 → 批量修改和导出 → 一键复制到主机维护表。

首版只在工作台内更新和保存图片，批量导出后由用户上传；不自动替换抖店商品详情页。编辑配置图中的业务字段必须回写同一份结构化配置，避免图文各维护一份。

已核实用户当前流程：勾选工具保存/调用模板 → 粘贴 F:H 到主机模板 → 人工补配 → V5 生成 ERP 横向 → 粘贴到主机维护表。新工作台整合中间转换，保留最终复制。规则与核验边界见[现有流程与工作台衔接](D:/工作/PythonProject/配置工具/现有流程与工作台衔接.md)。

## 优先执行顺序

| 顺序 | 对应任务 | 可检查的结果 |
|---|---|---|
| 1 | S0-01、S0-02、S0-05 | 依据已提供配置、详情图和维护表截图核对 V5 输出，并登记真实机箱素材 |
| 2 | S1 最小基础、S3-01 模板调用、S3-03 独立编辑、S3-06 | 配置在线修改后保存，重新打开不丢失 |
| 3 | S4-01、S5-01、S5-02、S5-03、S5-05 | 改配件后预览立即更新，保存后自动生成并存储相同版本图片 |
| 4 | S3-04、S5-04 | 批量改配后只更新受影响图片，导出文件数及失败清单可核对 |
| 5 | S4-05 | SPU、SKU ID、简称及配件区域分别复制，行数/顺序可核对；实际宏表粘贴由用户本机试用验证 |
| 6 | S2 及其他待办 | 补齐 ERP 成本、含税单价、核算、框架复用、完整迁移和最终验收 |

S4-05 可在 S4-01 后提前实施，无需等待批量图片功能完成。成本与库存继续保留，不作为首轮图文流程的前置。

## 已确认的规则

- ERP 成本由采集脚本一键更新，记录成功时间；失败保留原有效数据与原成功时间。
- 实际核算价即 B 列含税成本，人工粘贴单件价；ERP 更新不覆盖人工含税单价。
- 含税行成本 = 含税单价 × 数量，整机含税总成本为行成本之和。
- 人工同步单独记录时间和操作人；只粘贴价格列时核对目标行和顺序。
- 一个商品链接支持多个配置，保留配件、数量、进阶、质保和升级说明；内部成本与利润不默认进入客户图片。
- WPS 截图只用于业务内容，工作台外观独立设计；商品图片模板以实际图或源素材核对。
- 图片显示配置版本、图片版本、更新时间和生成状态；旧任务不能覆盖新图，生成失败不能显示已同步。
- 维护表复制按现有工具与目标表格式适配，不以普通文本/ERP CSV 代替；复制成功不代表外部表已写入。

本次已确认：26 张样例表、参考链接对应 X400 白页、19 行配置块、当前含税总成本 × 1.04 + 100、到手价减该基数的 EXCEL 利润、到手价 × 0.98 减 ERP 总成本的 ERP 利润；整数显示不等于计算先取整。旧模板另有 2% 规则。

待核对：真实配置的 V5 浏览器复制结果、数字 SKU ID 对应入口及完整列映射、部署和账号权限、人工价格生效范围、ERP 脚本契约与旧 2% 规则处置。来源表 AA/AB 存在 ID/数量错位，不能直接照搬。详细口径见实施计划及样例分析，已确认项不重复询问。

## S0 规则与样例基线

优先执行 S0-01、S0-02、S0-05，核对旧工具、维护表和配置图。S0-04 核算工作继续保留，但不阻塞图文主线。

- [ ] **S0-01 确认输入资产**：定位当前 V5.0 HTML、框架勾选工具、三店 CSV/JSON、成本源与实际链接 XLSX
  验收：每个资产记录来源、用途和版本；原文件不改写。
  进展：2026-09-14 已取得精简 XLSX 并记录 SHA-256；现已取得框架勾选工具和粘贴工具 V5.0，已只读核对并记录哈希；三店完整迁移导出尚未取得。

- [ ] **S0-02 固化旧规则**：提取粘贴解析、分类和数量、命名、ERP 横向/明细、Canva SKU 规则
  验收：至少 3 份真实样例覆盖独显/无卡、多数量、进阶版、未识别项，保留期望输出。
  进展：已核验 19 行结构、E 列数量、A/B 行成本、K:N 展示与升级、Q:T 名称辅助及部分 U/V 扩展；已提取 V5 横向输出纯函数，用两套合成配置核验每行 56 列及数量/空槽位规则；完整真实解析及浏览器复制尚未验收。

- [ ] **S0-03 定稿关键口径**：处理第 2 节问题；补充字段字典、状态流与验收数据规模
  验收：无影响建模的身份歧义；未决项有影响范围与最晚决策阶段。

- [ ] **S0-04 核验 ERP 脚本与核算公式**：读取采集脚本契约和成功/失败输出；人工含税单价按数量汇总，核对 ERP 金额单位及原表利润公式
  验收：明确执行位置及账号/店铺身份；人工价格范围及剩余公式有真实依据；补充估时。
  进展：X400 白 20 个配置块的当前汇总公式已核验，源单价与数量关系一致；发现旧 2% 规则，ERP 脚本与规则处置仍待核验。

- [ ] **S0-05 核对图片与维护表样例**：获取一个实际配置图或源模板；核对旧工具输出及维护表目标区域；首版限定系统内保存和批量导出
  验收：明确图片字段绑定、维护表列序/空列/公式边界；形成一份输入与预期图/粘贴结果样例；不以导出成功表示商品页面已替换。
  进展：已定位 `X400白3797170100491124774` 及前两套配置；工作簿未包含内嵌图片。辅助 ID/数量输出有错位，V5 已确认输出无表头、每配置一行 56 列 TSV，包含名称查找公式；目标表实际粘贴和图模板尚未验收。

## S1 工程与数据基础

先提供首轮需要的账号、店铺、配件与版本基础，后续逐步补齐。

- [ ] **S1-01 工程骨架**：按 PRD 候选栈确认框架组合、路由、环境配置、数据库连接和构建脚本
  验收：开发启动、类型检查和生产构建可执行；不混用两套服务端入口。

- [ ] **S1-02 账号与店铺**：登录/退出/会话、基础角色、shops 和服务端店铺范围校验
  验收：未登录无法访问数据接口；跨设备读取同一数据；权限规则有实际验证。

- [ ] **S1-03 配件主数据**：配件、别名、来源、分页搜索、分类、详情与停用
  验收：goods_id/名称/alias 可检索；别名歧义返回候选；被引用配件停用后历史仍可读。

- [ ] **S1-04 写入与版本基础**：import_records、change_logs、revisions、草稿版本及并发版本检查
  验收：写入成功才产生对应有效版本；两设备基于旧版本保存时能提示冲突。

## S2 导入与成本中心

成本模块继续保留。依赖 S1 与已核验脚本；不再作为完成在线配置、图片保存和维护表复制的必经前置。

- [ ] **S2-01 输入适配**：paste/xlsx/csv 标准接口、双层表头、字段映射、原行保留
  验收：字符串 ID 不失真；无公式缓存值明确提示；中文/内部表头均能识别。

- [ ] **S2-02 配件匹配**：goods_id 精确匹配、别名、候选、人工绑定与记忆
  验收：空 ID 成本源行标无效；ID 变化不凭名称换绑；同名冲突不自动选中。

- [ ] **S2-03 Diff 预览**：新增、成本、库存、配置、疑似删除、冲突分别呈现
  验收：显示旧值/新值/来源/店铺/对象；确认前不改业务数据，可保存导入暂存记录。

- [ ] **S2-04 应用与撤销基础**：事务写入、重复请求处理、确认时版本复查、批次追溯
  验收：同一应用请求不重复改数据；失败无半批业务写入；过期 Diff 需重算。

- [ ] **S2-05 成本中心页面**：上传/粘贴至确认完整流程、三店快照与基础预警
  验收：A 店缺货不改变 B 店判断；缺记录显示无数据；空成本不覆盖已有数值。

- [ ] **S2-06 ERP 脚本一键更新**：调用采集脚本、批次状态、字段与完整性校验、有效快照更新、结果摘要
  验收：一次点击完成正常更新；重复点击不并发重复执行；失败保留旧数据与旧成功时间；异常映射进入处理清单。

- [ ] **S2-07 实际核算价格列同步**：人工含税单价存储、标识/行范围对齐、差异预览、确认与同步时间
  验收：单列粘贴不发生错行；按含税单价乘数量得到行成本；ERP 更新不覆盖人工含税价；空白不清空；变更可追溯。

## S3 框架与在线配置

首轮包含 S3-01 的共享模板保存/调用、S3-03 的独立配置编辑及 S3-06；框架继承与 CPU×显卡组合再按依赖补齐。S3-07 核算接入 S2。只完成首轮子集时不勾选整项。

- [ ] **S3-01 整体框架**：共享模板保存、勾选现成配置生成草稿、框架列表、公共配件、差异项、复制/归档、草稿与不可变发布版本
  验收：草稿不改变已引用商品；复制成为独立框架；发布展示影响范围。

- [ ] **S3-02 组合生成**：按分类勾选候选、组合预览、逐条调整、重复和必填校验
  验收：2 CPU × 2 显卡可生成 4 个候选草稿；保存前排除重复并处理无卡语义与异常。

- [ ] **S3-03 商品与继承**：商品 CRUD、独立/框架创建、SKU 复制、固定基线、override、恢复继承
  验收：旧商品读取原基线；局部修改保留；恢复继承回到该商品选定的基线版本。

- [ ] **S3-04 更新与批量修改**：框架 Diff、批量接受、按配件/CPU/GPU/机箱筛选替换
  验收：先列影响链接和 SKU；冲突与 override 可辨别；确认后生成新版本和审计。

- [ ] **S3-05 进阶与升级**：基础/进阶关系、升级价差与文案、底部加购项目及价格单位/推荐数量/排序/显示开关、SKU 元数据、共同项和差异项
  验收：+199/+699 等含义保留；自动名称可手动覆盖并标记；不重复保存失联配置文本。

- [ ] **S3-06 编辑状态**：编辑态与服务端态分离、自动保存、保存时间、至少 20 步撤销/重做、切换恢复
  验收：不同 SKU 草稿不串内容；显示他人更新通知；两设备冲突不静默覆盖；撤销与持久版本恢复分开。

- [ ] **S3-07 链接配置核算展示**：配置逐项成本与数量、两套成本汇总、定价、利润、进阶与质保说明、来源更新时间
  验收：单价/行合计不重复计量；更换配件后引用正确价格；公式按已确认口径计算；外观不复刻截图。

## S4 兼容输出与试用

优先 S4-01 统一输出模型和 S4-05 维护表复制；复制不必等待图片模块全部完成。其余旧输出与影响分析继续保留。

- [ ] **S4-01 统一输出数据**：最终配置、数量、升级项、显示名、来源与版本组成统一输出模型
  验收：ERP、Canva、文本、后续图片都从同一份已解析配置取数。

- [ ] **S4-02 旧输出兼容**：ERP 横向/明细、规范/紧凑名称、Canva CSV/SKU、复制纯文本
  验收：同样例逐项核对套餐数、警告、数量及所有输出；每个差异有解释。

- [ ] **S4-03 真实配置试迁移**：接通至少一个真实商品配置块的解析与确认写入，覆盖 19 行及标题辅助识别
  验收：不要求用户先改为长表；未识别行可修正或退出；不静默丢行。

- [ ] **S4-04 影响范围与试用**：接通配件 usage、店铺预警下钻，试跑每日更新、建链接、改配与导出
  验收：库存、继承和覆盖后的实际配置一致；运营能完成一次日常流程。

- [ ] **S4-05 主机维护表一键复制**：以统一输出模型提供 SPU、SKU ID、简称列及 V5 兼容的无表头 56 列配件 TSV，支持单配置/多配置按区域复制；保留 ID、名称公式、数量、备注槽位
  验收：先核验各输出区域行数、顺序、长 ID 文本、配件、数量和必要空列；由用户在本机维护表试用确认粘贴结果；保留现有结构化查找公式并实表验证外部引用；空槽位兼容输出 ID=0、数量=1；复制失败有明确提示；不把复制等同于已写入维护表。

## S5 详情图生产

紧接最小在线编辑及 S4-01 实施；先完成一个真实模板的编辑联动、PNG 生成和自动保存，再扩展模板与批量导出。

- [ ] **S5-01 实时预览与模板**：浅色/深色两套视觉主题；单 SKU、多 SKU 对比、基础配置加升级项三种模式；配件行下升级说明、底部加购区与可选福利点模块
  验收：修改字段无需保存即更新预览；长名称、缺可选项、多配件不裁切。

- [ ] **S5-02 参数与资产**：字体、字号、字重、分区文字颜色、间距、字段顺序、背景、Logo、浅深主题、模板版本和素材存储；右上角 1:1 机箱图支持透明 PNG/白底图，等比完整显示；配置图字段绑定及点击定位编辑
  验收：配件文字修改回写对应配置字段，样式修改写模板；保存新模板版本；中文字体和素材可用；不可编辑位图需先重建模板。

- [ ] **S5-03 单张 PNG**：固定渲染输入、字体与图片就绪检查、倍率和尺寸校验
  验收：支持详情长条图与 1:1 方图切换导出；方图保留 1400/2800/4200 规格，长图宽度可设、高度按内容自适应；内容与所选版式预览一致；记录配置版本、模板版本及版式。

- [ ] **S5-04 批量 PNG**：多 SKU/多链接、今日上新/历史筛选、路径命名、重名处理、进度和失败清单
  验收：文件数、顺序、命名可核对；同名不覆盖；失败可定位和重试，不冒充全成功。

- [ ] **S5-05 配置保存与图片自动更新**：配置保存提交后触发图片任务，绑定配置/模板版本；去重、合并快速连续更新、云端文件保存与状态反馈
  验收：预览实时变化；图片生成后自动保存并记录时间；旧任务晚完成不覆盖最新图；失败可重试且不显示已同步；批量改配只刷新受影响图片。

## S6 迁移与正式验收

完成核心流程后，补齐完整迁移和正式验收；首版导出图片后由用户上传，不自动替换抖店详情图。

- [ ] **S6-01 旧工具完整迁移**：JSON、三店记录 CSV、配件目录/成本源、模板链接与名称设置
  验收：保留来源和店铺；模板链接仅作参考；不导入浏览器文件句柄和临时状态。

- [ ] **S6-02 WPS 迁移完善**：工作表角色、块范围、公式结果、升级区、辅助列、人工修正
  验收：核心样例可用；不规则表可手选/修正；失败报告含原位置与原因。

- [ ] **S6-03 运营与历史收尾**：Dashboard、规则提示、审计筛选、版本 Diff 与恢复
  验收：指标可下钻；框架更新与来源更新分开；恢复生成新版本，不改写历史。

- [ ] **S6-04 迁移与恢复验收**：三店逐项对账、批次撤销、失败恢复、备份恢复演练
  验收：记录/SPU/配置/ERP 行/未匹配数量均对得上；原数据保持可用。

- [ ] **S6-05 部署与交接**：启停/升级/备份方案、中文使用说明、真实流程验收记录
  验收：新环境可按说明启动；另一设备可登录读取；配置与模板资产均可恢复。

## 首轮验收与进度维护

用一个真实链接修改主板和风扇数量，核对在线配置、即时预览、已保存图片和维护表复制内容一致；再对两个测试链接批量改配并导出。模拟连续保存与图片生成失败，确认旧图不覆盖新图、状态不误报成功。

每项按全部验收条件完成后勾选并更新顶部进度。缺样例或待口径时在对应任务下记录具体原因；日期和工期在真实样例、脚本与模板核验后重新估算。完整功能验收场景见实施计划第 6 节。


### 配置图设计补充（2026-09-14）

英特尔官旗模板参考用户指定的 Intel 官网，追求简约、清晰、有留白；浅色与深色使用同一配置数据。机箱素材固定在右上角 1:1 区域，支持透明 PNG 或白底方图，等比完整显示。最新确认：整张配置图支持详情长条图与 1:1 方图两种版式，商品详情页配置单默认使用长条图；机箱素材仍使用独立的 1:1 区域。方图保留 1400/2800/4200 规格。

配件行下展示对应升级选项；清单底部另有独立加购区，支持价格、单位、推荐数量、说明、排序和显隐，例如“+39 元/把 加购 ARGB 风扇”“推荐 3/5/7 把”“支持 DIY 改配，联系客服核算差价”。尚未选中的加购不自动进入基础成本或 ERP 配件输出；实际选入配置才改变配置数据。

字体、字号、字重和颜色可按标题、配件名称、类别、升级说明、加购区分别修改，样式随模板保存并应用到导出图。浅深主题与业务配置版本分别保存，避免换主题改变配件。用户“一般两个版本”目前以浅/深两种设计提供例图；业务版本数量不限定为两套。设计例图中的机箱仅作占位，正式模板绑定真实机箱素材。


### 长条图、方图与模块化编辑（2026-09-14，当前有效）

导出版式可切换“详情长条图”和“1:1 方图”，浅色与深色均支持两种版式。详情页配置单使用长条图，不将方图作为详情页默认输出。去掉配件类别旁的装饰图标，仅保留 CPU、散热等类别文字，以窄类别列和宽型号列显示。长型号自动换行、行高随内容调整；升级说明紧随所属配件。长条图固定所选宽度，高度随完整内容增长；方图保持 1:1，并按方图布局重新排版。两种版式均不能省略或裁切型号，也不能拉伸原图完成比例切换。方图布局超限时明确定位溢出模块，提供调整字号、行距、模块间距或切换长图的操作，处理后再正式导出；不得通过无限缩小文字掩盖溢出。

整张配置图按模块编辑：店铺/标题、配置编号与版本、右上角机箱图、配件清单、随配件绑定的升级说明、加购与选配、福利点、页脚说明。支持选中定位、增删允许的模块、拖动排序、显示开关、间距/对齐/配色/字体/字号/字重调整、撤销与模板保存。机箱模块默认锚定右上角，清单数据字段编辑仍回写配置，纯样式写模板；升级说明不可脱离对应配件而错绑。

福利点模块默认位于加购区下方，支持多条短文案、排序、显隐、背景及文字样式，初始为空，由用户维护实际承诺；例图仅用占位文案，不把示例福利自动应用到商品。加购和福利独立保存，未选购的项目和福利文案不计入基础成本或 ERP 配件输出。多套模板和浅深风格均保存模块结构与样式版本，批量生成遵守同一布局校验。


导出交互：先选择“详情长条图 / 1:1 方图”，再选择“浅色 / 深色”、尺寸及导出范围。默认详情长条图；切换后立即预览对应排版。两种版式共享配置内容，分别保存模块布局参数，避免改一次文案需要维护两份。批量导出按所选版式执行，文件名包含版式和主题，防止相互覆盖；单张与批量均校验内容完整、尺寸及溢出。


### 主机维护表与真实素材补充（2026-09-14，当前有效）

用户确认维护表人工维护范围为 SPU、数字 SKU ID、简称和展开的配件列。用户已明确：原表带宏且数据量大，暂不提供副本。不再把取得维护表副本作为需求整理、原型或功能开发的前置；不读取、修改或执行该宏工作簿。依据提供的截图和 V5 源码建立输出样例，由用户在本机试用时确认实际粘贴结果，未完成该步骤不得宣称宏表联调通过。

输出应支持复制 SPU 列、SKU ID 列、简称列和配件横向区域；这些输出共享同一配置集合及顺序，逐列行数一致。支持批量粘贴/维护 SPU 和真实 SKU ID，以字符串保存；空值明确标记，不能用配置序号、名称或随机值代替真实 ID。截图可见 SPU 位于 G、SKU 位于 H、简称位于 O、配件区域自 P 起；中间有隐藏/辅助列，所以不能直接把三个字段与 56 列拼成连续 59 列后声称可整行粘贴。若以后增加整行复制，应先明确完整物理列映射；当前保留按目标区域一键复制的方式。

源码证据：V5 的 ERP导入-配置简称支持两种名称格式及无表头复制（1770—1792、4837—4843 行），展开配件为无表头 56 列 TSV（4037—4092、4871—4880 行）；SPU 在记录中输入/保存。当前提供的源码尚未核实独立数字 SKU ID 的复制入口，pkg.sku/Canva 的 SKU简称不能据名称当作平台 SKU ID。用户现有操作作为兼容目标保留，原型补齐明确的 SPU/SKU ID 输入及复制入口，后续用实际操作核对，当前不要求再次提供工作簿。

已收到真实机箱素材：`D:/工作/主机详情页/X400 CG/白色/A-主图/DSC05649-已增强-NR.png`，2278×2277、RGBA，存在透明像素。原型按 1:1 素材位等比完整放置，1 像素尺寸差通过容器留白处理，不拉伸或裁切原图。它是机箱展示素材，不能从照片推定实际配置清单中含有所有可见硬件。


### 方图导出策略更新（2026-09-14，覆盖先前方图溢出阻止规则）

用户要求自动处理，允许白边补齐。当前原型默认：先排出完整配置长图，再等比缩放到所选正方形画布内，居中用纯白留边补齐 1:1；浅深主题均保留全部配件、升级、加购、福利和页脚，不裁切、不拉伸、不再因长内容阻止方图导出。长图仍按内容高度原样导出。方图预览与单张/批量导出采用同一处理，并将点击模块的坐标同步映射到缩放后位置。


### 可操作原型交付（2026-09-14）

已创建 `prototype/`，启动入口为 `prototype/启动原型.cmd`，地址 `http://127.0.0.1:4178/`。包含 20 套真实配置编辑、配件选择、模块样式/顺序/显隐、长图与白边方图、浅深主题、PNG/ZIP、V5 简称及维护表分区复制、模板保存调用、成本粘贴/差异导入、本地持久化和保存版本冲突检查。范围与操作见 `prototype/使用说明.md`，证据及未验收部分见 `prototype/验收记录.md`。这不是生产多人系统，不将跨任务的原型子集当作完整 38 项实施任务全部完成。

### CloudBase 多人同步实施进度（2026-09-16）

个人版已核验生效；正式编辑器已接入 PostgreSQL 协议 2、SQLite 草稿/队列、共享成本、配置/模板/设置/输出源/图库、完整 ERP 快照和私有原图。右上角新增同步按钮，Windows 桌面实测版位于 `release/DIY配置工作台-0.2.0.exe`。91 项本地测试、6 项正式云端测试通过，119 条正式配置只读对账无差异。正式数据未上传，首次导入/发布由用户在实测界面确认。公司 SQL 保留为新增来源，现有脚本继续可用；SQL 实际连接、小程序和定期云端备份/素材清理仍待后续。完整边界与操作见 `prototype/多人同步使用与验收.md`。


### 2026-09-16 桌面 0.2.2 加购维护

新增桌面图标、加购商品与整数分报价关联、ERP/核算双差价提示、输出源一键检查、批量加购描述修改和联动替换预览。100 项单测与浏览器/桌面各 5 项流程通过。当前旧手动加购不会自动猜测归属，须用户明确选择关联配件与商品。交付及使用说明见 `prototype/多人同步使用与验收.md`，EXE 为 `release/DIY配置工作台-0.2.2.exe`。
