# 原型验收记录

## 2026-09-15 WPS 整块表格分组与空值修复

用户实际一次粘贴了多套 19 行配置，此前实现仅支持单套，因此产生汇总行 ID 报错和跨配置重复分类。已只读核对桌面旧「框架配置勾选工具_新.html」的 `BLOCK_HEIGHT=19`、`normalizeRows(..., true)`、`parseSource`，以及旧粘贴工具的组内第 3–10 行分类和 D/E 配件来源。

- 改为保留空行位置分组，再跳过表头、汇总、空行和名称为空/0 的配件占位行。每套独立校验槽位，可一次创建多套同链接配置，原 ID、数量与名称不串组；非空配件的真实 ID/数量错误仍定位原行并阻止创建。
- 支持三列、D:H 及从 A 列开始的完整区域。截图 F 列为空，自动使用 E 列；完整表格可读取 K/L 中的配置名称与版本。每套单独保存原始文本切片和起始行号。
- Node 测试 48/48。依据用户两套截图内容构造 40 套、760 行测试样例，识别 400 项配件，跳过 360 行：40 表头、40 汇总、120 空行、160 空配件；不是对用户未提供的实际 760 行粘贴文本逐行核对。
- 独立 4191 服务实际预览、批量创建、保存并生成 40 张 PNG，刷新第 40 套后保留 10 项配件、风扇数量 11；所有组保持同一商品链接和 SPU，SKU 留空。原有测试配置完整保留。浏览器无脚本错误，证据 `verification/wps-ui-result.json`、`verification/wps-40-block-preview.png`。
- 日常状态文件前后 SHA256 一致：`39838C70B71FBF02AE32402064DB4AAAE6090DBB1260739B8F145996088C17D8`。新前端脚本已由 4178 提供，刷新页面生效，无需重启服务。

## 2026-09-15 修改.doc 14:55 续改

依据用户更新的修改.doc（157696 字节、14:55:28），只读提取文字和四张截图至 `verification/change-notes-20260915-1455/`。实现固定总览、简化简称操作记录、WPS 三列及 D:H 配件粘贴导入。

- Node 测试 44/44，通过长 ID、原文名称、精确成本匹配、E/F 数量选择、两项风扇、无卡与空槽、带引号多行单元格、错误行和未知分类检查。
- 独立 4191 服务、`verification/wps-import-data`：使用实际浏览器剪贴板粘贴 11 项配件，预览不写入；手动指定未识别的散热分类后创建，生成图片并实际落盘；刷新后 11 项配件、风扇数量 8、19 位 SPU、空 SKU 和原始粘贴文本保持。现有配置不受导入覆盖。
- 五列区域切换 E/F 后预览数量分别为 1/2；非法数量不能创建，未知分类可手动选择。新日志写入「生成简称」，历史「按 V5 规则生成简称」仅显示为「生成简称」，未改写历史日志正文。
- 总览列表滚动 476px，标题纵坐标 99px、复制工具栏纵坐标 206.59375px 保持不变；弹窗自身 scrollTop=0。桌面和 500px 窄屏截图检查通过，1280/768/500px 无页面或弹窗横向溢出，导入预览表可独立横向滚动。
- 浏览器无脚本错误；正式 state.json 前后 SHA256 均为 `39838C70B71FBF02AE32402064DB4AAAE6090DBB1260739B8F145996088C17D8`。本次没有重启日常服务或写入正式配置，4178 新脚本接口返回 200。完整证据见 `verification/wps-ui-result.json`。

日期：2026-09-14。

- Node 语法检查通过；8 项核心测试通过。ERP 56 列逐格对比本任务此前提取 V5 纯函数保存的期望输出，包含 ID、查找公式、数量和空槽位规则。
- 真实首配置核算：ERP 6520、含税 6440、核算基数 6797.60、EXCEL 利润 -98.60、ERP 利润 45.02；电源数量 1、风扇 8、内存 2 对应同一配件 ID。
- 浏览器中载入 20 套真实配置，检查桌面三栏和窄屏布局；编辑与保存后实际生成了 PNG 文件，并打开检查真实机箱、长名称、升级说明及底部加购区域。
- 浏览器验证数量输入从 8 改为 7 后，展开复制的风扇数量为 7、列数为 56；格式二简称为“配置1：14600KF+无卡+32G+1T丨进阶版”；缺少 SKU ID 时复制按钮禁用。
- API 验证过期版本提交返回 409，两个实例中的当前版本号未被测试请求修改。
- 根据用户反馈，将原先方图溢出阻止改为完整内容缩放＋白边补齐。原生 Canvas 使用生产 poster.js 生成浅色/深色长图及方图：方图均为 1400×1400，边角为纯白，所有模块在画布内；对应长图 1400×2318。实物 PNG 已打开视觉检查，底部福利完整保留。
- 批量 ZIP 由生产 zip.js 生成，Python zipfile 解包及 CRC 检查通过，包含四张 PNG 和中文导出清单。证据：verification/render-results.json、verification/export-check.zip。

限制：最后一轮浏览器连接中断，因此白边修订后的浏览器最终交互复验未取得完整结果，采用原生 Canvas 同代码验证补充；不能据此宣称已完成全部浏览器端回归。未接触宏工作簿，未验收其实际公式计算。多人登录与 ERP 采集不在已完成功能中。

### 启动入口修复（2026-09-14）

- 中文 CMD 入口改为纯 ASCII 内容与 Windows CRLF 换行，调用 `start-prototype.ps1`，避免代码页切换和中文脚本名参与批处理解析；原中文 PowerShell 入口保留兼容。
- 实际通过 `cmd.exe /d /c .\启动原型.cmd` 执行，退出码 0，输出“工作台已启动”；4178 状态接口可访问，返回 20 条配置、revision 11。
- 本次验证复用了已运行服务，未测试冷启动；未修改配置数据。


### 《修改.doc》与模板删除（2026-09-14）

已实现新增/编辑商品链接、分组折叠、空白配置、整组模板保存、模板内多选导入、可恢复的模板删除、配置总览、到手价上移与编辑、生成简称按钮重命名、配件文本框自动高度。兼容旧单配置模板和未带 productId 的原型数据。

验证证据：

- 12 项 Node 测试全部通过，包含原有 8 项及链接迁移、整组模板快照、多选导入身份隔离、空白配置初始化。
- 浏览器隔离数据：20 套原始样例保存为整组模板；新建链接后选第 1、3 套导入，总数为 22，无多余空白配置；两套配置 SKU 均为空，SPU 为目标链接字符串。
- 删除整组模板后，已生成的 22 套配置保留；刷新后仍显示一个已删除模板，恢复后重新可选。支持整组 20 套全选和 19 套部分勾选状态。
- 总览显示当前链接两套搭配摘要；到手价 6999.88 可保存；长名称文本框从 34px 增至 274px，内容未截断；链接折叠状态正确。
- 桌面 1440px 和窄屏实际 500px 无横向溢出，模板弹窗显示正常；浏览器控制台无错误或警告。
- 本次浏览器写入由隔离 fetch 存入测试标签页 sessionStorage，图片保存响应也为模拟。测试未写入正式数据；后端保存接口未改动，因此本次不声称重新验证了真实磁盘写入或冷启动。
- 启动独立测试服务被自动审批拦截，未提供具体原因，随后采用上述隔离测试。

原修改文档的只读转存及其配图保留在 `verification/change-notes/notes.html`，便于核对；桌面原文档未修改。


### 配置删除与紧凑利润栏（2026-09-14）

只读核验 `英特尔 主机 配置[在线].xlsx` 的 `X400白3797170100491124774`：A2=`SUM(A3:A17)`，B2=`SUM(B3:B17)`，C2=`B2*1.04+100`，D2=`G2-C2`，E2=`G2*0.98-A2`；A3/B3 的单价查找结果均乘 E3 数量。其他三个 X400 工作表的同位置公式一致。

- 15 项 Node 测试通过；新增删除/恢复完整数据、原表利润数值、全删后新建不继承删除标记。
- 隔离浏览器使用 4 套测试配置、2 个链接，验证当前删除后自动切换、撤销、按链接全选删除、顶部全选删除；全部删除后 0 套空白页正常，刷新仍为 0 套。
- 全删后可以新建链接；4 套原配置均可恢复，恢复后保留 7999 到手价和原配件。
- 到手价 7999、ERP 成本 6520、含税成本 6440 时，EXCEL 利润 1201.40、ERP 利润 1319.02；CPU 数量从 1 改为 2 后，分别为 -254.60、-80.98，界面即时更新。
- 到手价输入框实测 106px；桌面截图已检查，无横向溢出，控制台无错误/警告。
- 交互测试通过 fetch 隔离到测试标签页 sessionStorage，未对用户实际配置执行删除；后端接口未改动，本次未重新验证真实磁盘写入。


### 输出源维护（2026-09-14）

实现独立导航入口、1189 条原始配件的搜索分页、精确 ERP ID 新增配件、名称/含税核算单价/升级/加购维护、字段与范围选择、逐项同步差异预览。输出源纳入原有状态保存、撤销和备份；ERP 缓存与数量、平台身份不随来源文案维护而修改。

验证：

- 18 项 Node 测试通过，包括重复 ID 的明确绑定、选择字段同步、手动加购保留、重复同步不重复添加加购、来源加购清空、已删除配置排除。
- 本次使用真实独立 Node 服务 4182，数据目录 `verification/source-maintenance-data`。ERP ID 47538 修改显示名称、含税单价 1400→1500、升级与加购后，“仅保存输出源”成功写入磁盘，原配置名称保持不变；刷新后可读取新值。
- 选择“左侧勾选的配置”同步，仅更新一套配置的四个字段，其他配置不变；ERP 单价 1400、数量 1 保留，原两条手动加购保留，追加一条带来源关联的加购。
- 实际生成 `images/x400-01_r2_long_light.png`（640807 字节）。
- 新增测试配件可保存；核算价格修改为 35 后撤销，服务器状态恢复 29.9。“选择配件”能选用维护后的名称和价格。
- 非法输出源价格返回 400；旧客户端未提供 sourceCatalog 时保留已维护的输出源；过期版本保存返回 409。
- 浏览器界面检查通过，控制台无错误或警告。
- 日常 4178 服务升级前备份 `.local/before-source-maintenance-20260914-181228.json`，升级前后 revision 57、22 条配置记录、1 个模板一致。测试数据未写入日常目录。


### 成本源与库存 / ERP 一键同步（2026-09-15）

- 24 项 Node 测试通过；涵盖精确 ID 合并、人工价及人工时间保留、未知库存、空 ERP 保留旧价、分页缺失和重复 ID 拒绝、账号仓库绑定、任务过期及回传防重。Node 语法检查通过。
- 独立 4183 服务，数据目录 `verification/erp-sync-data`，未向日常 `.local` 写入验收商品。浏览器验证成本源搜索、单条价格保存、两列批量粘贴、全选后单列粘贴（含空行保留），保存后刷新数据保持。
- 单条 47538 从 1400 修改为 1550，5 套引用配置更新并记录时间；两列粘贴更新为 1560，47523 设为 0。模拟 ERP 回传 47538 库存 12 / ERP 成本 1300 后，人工价 1560 和原人工时间保持不变。
- 模拟 47523 库存、ERP 成本均为 null：库存显示“未知”，ERP 保留 1450 并标注“保留旧值”；人工价 0 保留。后续全选单列粘贴的第一行空白保持 47538 原价，第二行更新 47523 为 25。
- 右上角按钮等待连接、成功显示时间、配置利润更新、5 张 r4 PNG 实际落盘均已验证。浏览器无控制台错误或警告，主界面及成本源界面无横向溢出。
- 不完整回传、重复回传被拒绝；重复成本 ID 返回 400，错误来源返回 403。失败前后状态响应逐字一致，上次成功时间保留。证据 `verification/erp-sync-verification.json`。
- 参照后台库存监控 `inventory-erp.user.js` 的公司大库识别、分页接口、可销库存字段和本机桥接方式；新脚本独立存放，原项目未修改。成本字段由 ERP 实际表格列定义识别，未猜测字段名。
- 当前实际 ERP 浏览器桥接未连接，未进行真实库存/成本采集；首次安装脚本后需在用户的 ERP 页面验证。模拟验证不能代替真实 ERP 验收。

日常实例已升级并启动于 4178：升级前后 revision 91、22 条配置、1 个模板，state.json SHA256 完全一致。备份为 .local/before-erp-sync-20260915-092712.json；脚本下载接口返回 200。

### 修复成本批量粘贴看似卡住（2026-09-15）

原因：原解析器将 goods_id / 含税成本以及单独的 goods_id 表头当作数据拒绝，错误信息位于 30 行表格下方，保存按钮禁用但用户看不到原因。

修复：识别表头及重复表头；错误和进度紧邻粘贴框；报告未匹配 ID、空价保留、相同重复合并，冲突重复价格指出原始行号并停止保存。预览最多绘制 100 条差异，同时显示完整应用数量；核算同步改为按 ID 映射遍历，避免每条价格反复扫描输出源。

28 项 Node 检查通过。隔离浏览器内存数据验证 16,110 行及两个表头：预览约 11ms、101 行预览文字（含截断说明）、预览无写入；保存只调用一次，全部 16,110 行更新；保存中显示进度、按钮禁用。非法价格提示第 2 行并显示在表格上方。浏览器测试使用内存应用回调，不提交用户的 /api/state；本轮未修改用户成本数据。日常 4178 已提供新脚本，无需重启服务。

### #VALUE! 使用 ERP 成本（2026-09-15）

按用户确认规则，#VALUE! 单元格改用准确 ID 对应的 ERP 单价作为核算价，预览显示替换标记及数量。ERP 缺失、未知、未匹配时不写入，保留原核算价。普通核算价等于 ERP 时照常应用；ERP 零价有效，保留原 ERP 数值精度。支持单列及两列；重复行价格冲突仍停止。

30 项测试通过，覆盖替换、时间、零价、精度、不可用成本、单列位置及冲突。浏览器使用隔离内存数据验收：ERP 120 替换 #VALUE!，无 ERP 的第二行保留 90，普通值 50 照常应用；替换仅在点击保存后执行。未向用户 /api/state 提交修改。


### 修改.doc 与后续版式反馈（2026-09-15）

只读提取用户 09:49:53 更新的修改.doc，包含 3 张截图。原文件保留；本次参照输出源右下角红框位置、固定方图、预览直接文字编辑、颜色预选与保存、机箱独立拖动缩放。随后按“太紧凑”“多采用圆角”“字体布局调整”的反馈改为双列圆角卡片：类别在上、型号在下、数量右上角，英文规格完整换行。

- 32 项 Node 测试通过，含来源删除恢复、英文规格完整换行。渲染检查覆盖浅深色及长/方图、20 套原始配置固定方图、长名称、福利模块、过多内容阻止导出。
- 方图都是 1400×1400，fit=1、padding=false；深色四角为主题深色，未加入白边。文字区域完整位于画布内。
- 4185 独立服务、verification/modification-ui-data：输出源实际删除及恢复落盘，删除时所有配置 JSON 完全一致；用户主实例未执行测试删除。
- 点击 CPU 型号后可以独立改颜色和字重，#ff7c36 / 800 保存到配置 textStyles；颜色保存至浏览器常用色。重新载入后控件回显正确。
- 浏览器真实拖动手势验证机箱图移动和右下角缩放：位置从 x552/y26 变化至约 x497/y61，size122→157.10。保存后 r11 PNG 实际落盘；同配置去除图片变换前后，全部文字区域数据逐项相等。
- 放大编辑后实际画布 CSS 宽度 700px，右侧样式区可编辑，无页面水平溢出。工作台各面板和按钮圆角已检查；浏览器控制台无错误或警告。
- 生成清洁示例 verification/square-design-light.png 与 square-design-dark.png；这两份示例不含额外长名称测试文字或福利测试文案。
- 新增功能为前端脚本和现有配置字段，日常 4178 服务直接提供更新文件，不需要重启；现有用户配置不自动重写，重新保存时按新样式输出图片。

## 2026-09-15 通栏方图、紧凑详情图与服务承诺

- 已按确认的浅色风格实现通栏配件行，并适配深色；方图 1400 × 1400，常规详情图 1400 × 1834。详情图遇到长文案可自然增高，完整保留内容。
- 服务承诺可在模块面板或预览检查器修改文字、背景、字体和字号，可排序、隐藏；旧模块只补入新模块，不覆盖原有设置。
- Node 测试 33/33；20 套真实样例方图无溢出。渲染检查覆盖文字对齐、数量不遮挡型号、长文本换行、服务模块换行/移位/隐藏、机箱移动不改变文字和极端内容阻止方图导出。
- 在独立数据目录 verification/layout-service-ui-data、端口 4186 验证服务文字和背景的编辑、自动保存图片、刷新保留、隐藏及恢复。两配置批量导出得到 2 张 PNG 与导出清单，无失败，尺寸分别为 1400 × 1834 和 1400 × 1860。
- 浏览器业务脚本无报错；独立服务存在既有 favicon.ico 404，不影响功能。实际 4178 服务已返回新静态代码，未重启服务，测试未写正式 .local 数据。
- 可查看 verification/approved-square-light.png、approved-square-dark.png、approved-long-light.png、approved-long-dark.png。

### 同轮补充：配件图片隐藏与批量样式

- 配件行新增「配置图显示」，仅渲染器筛除隐藏行；成本、ERP 横向输出、配件编辑和模板仍使用完整配件记录。
- 「批量配色与格式」支持已勾选或全部配置，配色/格式/机箱位置分别控制；提供整体调色区。样式同步按配件槽位和模块对应，不覆盖配件、文案、ID、核算价格、机箱素材或各配件隐藏状态。
- Node 测试 36/36。新增隐藏后成本与 ERP 输出保持、模板保留隐藏标记、跨配置样式对象隔离、仅配色/仅格式范围验证；浅深主题与两种版式均不输出隐藏配件。
- 独立 4187 服务、verification/hidden-style-ui-data 浏览器验证：隐藏风扇保存后，两种版式均不含风扇；成本与维护表输出逐项一致。向选中的前 2 套应用配色字体，第三套完整 JSON 保持不变。
- 全部范围再次应用到 3 套并保存，所有配色字体一致、各套配件 JSON 保留；刷新后隐藏开关仍取消勾选。正式 .local/state.json 校验值 D4736F25CE575410FEF6FA992872A6EC8FF4D5BC0835431A753EDB89DADDF5B9 在本次浏览器测试前后保持一致。
# 2026-09-15 修改.doc 14:31 续改

只读提取桌面《修改.doc》的 6 张截图及文字，保存在 `verification/change-notes-20260915-1431/`。用户另确认分期手续费按到手价计算。实现总览分别复制简称和定价、店铺统一券额、逐配置分期核算、配件行布局、启动期间删除记录、操作人标记。

- Node 测试 39/39 通过；新增定价小数、零价、12/24 期手续费及利润、复制顺序和启动期间删除筛选检查。
- 独立 4188 服务、`verification/pricing-session-data` 浏览器验证：20 行简称和 20 行定价复制到实际浏览器剪贴板、操作人保存、优惠券及分期刷新保持、配件按钮可用、删除/恢复及刷新、成本核算页。浏览器无脚本错误。
- 样例到手价 7999、券额 200，定价 8199；12 期手续费 479.94，EXCEL 利润 721.46、ERP 利润 839.08。24 期手续费 799.90。
- 1440、1280、768、500px 无页面横向溢出；已检查桌面配件对齐、蓝色按钮、总览列与窄屏截图。价格区采用四列上排、分期与两项利润下排。
- 独立 4189 服务、`verification/pricing-server-data` 验证实际磁盘保存、操作人回读、非法券额 400、过期版本 409、旧客户端省略券额时保留设置，以及真实重启后删除列表为空且磁盘业务数据完全相同。证据见 `pricing-server-result.json`。
- 日常 4178 服务的重启被自动审批拒绝，未提供具体原因；未执行替代方式停止服务。已提供 `重启原型.cmd`，脚本通过 PowerShell 语法检查，但未执行。用户需保存当前编辑后运行一次，再刷新页面。旧服务启动新页面时会明确提示重启。
- 日常 state.json 在测试前后 SHA256 均为 `FB953FAF3E9F90A1448315D95B0684BBA441D739017314324037AFA7AFEEA4C2`，revision 199，未写入测试数据。升级备份路径记录在 `verification/pricing-upgrade-result.json`。

### 重启入口兼容修复（2026-09-15）
用户双击时出现命令截断。原入口为 UTF-8 中文正文及 LF 换行，现改为纯 ASCII、Windows CRLF，并调用英文命名的 restart-prototype.ps1；中文 .ps1 保留兼容转发。已通过真实 cmd.exe /d /c 执行原中文路径的重启原型.cmd -CheckOnly，退出码 0，检查 PowerShell 语法及路径成功。仅验证启动链路，未重启日常服务、未写业务数据。


### 日常启动实际恢复（2026-09-15 14:47）
关闭网页后旧服务 PID 58348 仍占用 4178，新版 /api/session 返回 404。实际运行中文重启入口、执行完整备份及重启成功；备份 before-restart-20260915-144703.json。新会话启动于 2026-09-15T06:47:06.280Z，数据 revision 199、23 条配置，SHA256 仍为 FB953FAF3E9F90A1448315D95B0684BBA441D739017314324037AFA7AFEEA4C2。真实 4178 页面载入配置、操作人、分期控件及 1400px 预览，浏览器脚本错误为零。随后去掉入口 Read-Host 等待，双击即备份重启；改后通过真实 cmd -CheckOnly 检查。

# 2026-09-15：维护表定价导入与批量换图验收

- `node --test prototype/tests/*.test.mjs`：52 项通过。新增覆盖 Markdown 首行数据、HTML 空格、负库存参考值、券后换算、重复/冲突/非法输入、预览过期时整批阻止写入。
- `node prototype/verification/maintenance-ui-check.mjs`：独立 4193 端口和临时数据目录，用户提供的 20 行简称/库存/定价全部匹配；到手价首行 6099、末行 25299，20 张配置图保存成功。另一链接的同名配置保持原样，配件和分期设置保持原样。
- 上传图片仅替换勾选的 2 套配置，其他字段与未选配置逐项一致；刷新后复用当前图片替换另一套成功。损坏图片阻止应用。预览和取消均不写入配置。
- 1440 与 500 像素宽度对话框截图检查通过，无页面脚本错误。测试前后日常 `.local/state.json` SHA256 一致。
- 证据：`verification/maintenance-ui-result.json`、`maintenance-price-desktop.png`、`maintenance-price-mobile.png`、`maintenance-image-desktop.png`、`maintenance-image-mobile.png`。测试数据目录验收后移入回收站；结果与截图保留。
# 2026-09-15：配置图自适应排版

- `node --test prototype/tests/*.test.mjs`：52 项通过。
- `node prototype/verification/adaptive-poster-check.mjs`：方图/详情图分别验证完整模块、隐藏加购福利、无页脚、仅六项配件、空模块、模块重排，共 12 种组合。方图 1400×1400，测试详情图 1400×1834；剩余高度分配误差不超过 1 个布局像素。
- 模块无重叠、文字在画布内、机箱素材自定义大小和坐标保留；空模块无绘制区域；过滤空加购条目后，原有文字样式仍对应原条目键。渲染不修改配置，日常数据文件哈希不变。
- 人工查看方图无加购福利、详情图仅配件模块两张输出，配件行均匀展开、数量标记和文字对齐。证据为 `verification/adaptive-poster-result.json` 与 `verification/adaptive-*.png`。
# 2026-09-15：滚动窗口固定顶部

`node prototype/verification/sticky-dialog-check.mjs` 通过：1440/500 宽度分别打开操作记录、配件目录、成本源、输出源、模板与配置总览。实际滚动操作记录 7158px、目录 1389px，标题坐标不变，关闭按钮仍可点击；搜索栏固定，配置总览复制栏不移动，无横向弹窗溢出和页面脚本错误。模板当前内容不足一屏，验证其正常显示与关闭。证据为 `verification/sticky-dialog-result.json` 和 `sticky-*.png`。
# 2026-09-15：全局批量修改与按链接打包

- 单元测试 55 项通过，新增全链接配件查询、按准确 ID 限定替换范围、预览过期整批阻止、数量与隐藏状态保留、两种图片唯一文件名及 SPU 检查。
- `node prototype/verification/global-batch-check.mjs` 使用独立 4194 端口和临时数据，3 个关联链接中勾选 2 个：实际仅修改 3 套，另 3 套保持原样；保存并刷新后 ID 正确。
- ZIP 实际解析得到 2 个链接文件夹、8 张 PNG（所选链接全部 4 套配置各两张），全部宽度 1400，SKU 图高度 1400，长 SPU 无精度损失，包含链接内未命中配置。无脚本错误，日常数据 SHA256 不变。
- 1440/500 宽度检查通过。证据保留在 `verification/global-batch-result.json`、`global-batch-desktop.png`、`global-batch-mobile.png`、`global-batch-export.zip`。两次测试的临时数据目录验收后移入回收站。
# 2026-09-16：商品链接导航

浏览器验证通过：展开第三个链接后位于列表首位，滚动 130px 后标题仍吸顶；浏览该链接不改变正在编辑的配置与卡片；搜索无结果后点击「定位」清除过滤并让当前配置回到可见区。1440×1000 和 500×900 检查无页面横向溢出、无脚本错误，未写入配置数据。截图为 `verification/link-navigation-desktop.png` 和 `link-navigation-mobile.png`。
# 2026-09-16：全部折叠与链接分类

55 项现有单元测试通过。`verification/link-category-check.mjs` 使用独立 4195 端口验证搜索中全部折叠、编辑对象不变、分类保存后刷新、自定义分类、筛选范围全选/取消、定位清除分类和搜索，1440/500 宽度均正常，日常数据哈希不变。额外验证空白配置与模板导入沿用目标链接分类，新链接不会误继承参考配置的分类。结果和截图为 `category-result.json`、`category-desktop.png`、`category-mobile.png`；临时测试数据已移入回收站。
# 2026-09-16：原位分类与颜色面板

`link-category-check.mjs` 已改用左侧分类标签原位编辑，验证分类及自定义名称保存、刷新和筛选通过。`color-picker-check.mjs` 使用独立 4196 测试服务验证升级颜色、按名称搜索、HEX 输入、收藏、配件底色保存刷新、撤销和 Escape 取消，1440/500 宽度面板在视口内，无脚本错误、日常数据未改动。修正选色后自动还原焦点造成多余撤销快照的问题。截图 `color-panel-desktop.png`、`color-panel-mobile.png`，结果 `color-result.json`；临时测试数据目录移入回收站。
