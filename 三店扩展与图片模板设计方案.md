# 三店扩展与图片模板设计方案

日期：2026-09-16。状态：设计方案，尚未实施；本文不代表现有工作台已支持多店。

## 1. 已确认需求

- 店铺：英特尔官方旗舰店、技嘉旗舰店、乔思伯官方旗舰店，均销售整机配置。
- ERP 数据与核算数据三店同步。
- 配件展示名称与加购内容可按店铺分别编辑。
- 配置清单图与 1:1 SKU 图需要按各店品牌风格设计。
- 用户指定配色：技嘉旗舰店深色版采用黑橙；乔思伯官方旗舰店采用黑白。两种图片均遵循对应店铺配色。

“共享核算数据”落实为共享配件核算单价和现有核算规则；各套配置仍按自身配件数量计算成本，按本店售价、券额和分期计算利润，不强制三店整机售价或利润相同。

## 2. 数据分层与修改范围

| 层级 | 内容 | 修改范围 |
|---|---|---|
| 共享配件成本源 | ERP ID、ERP 原始名称、ERP 成本、库存、人工含税核算单价、来源与更新时间 | 三店引用同一配件的数据同步 |
| 店铺输出源 | 展示全称、方图展示简称、升级文案、加购项目及说明、展示用质保文字 | 仅当前店铺；同步到本店配置前显示差异 |
| 商品与配置 | 商品链接、SPU、真实 SKU ID、配件组合、数量、售价、券额、分期、配置专属文案 | 各店独立 |
| 品牌与图片模板 | Logo、店铺标题、配色、两种图的布局、服务承诺 | 各店独立；模板升级选择应用范围 |

共享键沿用真实 `goodsId`，并保留已验证的 ERP 账户/仓库来源。不同 ERP 来源不能只凭相同 goodsId 混合。

店铺输出源保留独立 `sourceId` 并关联 `shopId`、`goodsId`。同一真实配件可有多个用途不同的展示条目，不强制把它们按 goodsId 合并成一条。成本都从共享成本源解析。

展示文字优先级：配置明确覆盖 > 本店输出源 > 共享原始名称。空值与“有意清空”分开存储；清空加购表示不展示，不能因下次同步重新出现。

### 同步行为示例

同一个 goodsId 在英特尔店、技嘉店、乔思伯店分别有不同名称和加购说明：

- ERP 同步：三店成本、库存一起更新；原始名称可以更新，三店自行维护的展示文字不变。
- 人工核算单价调整：先显示旧值、新值和三店受影响配置数量，再保存到共享成本源；按各套实际数量重新核算。
- 技嘉店修改名称或加购：只影响技嘉店选定范围，不更改另外两店。
- 新建或跨店复制配置：解析当前共享成本；采用目标店输出源与图片模板，不复制来源店的 SPU/SKU、价格、服务承诺与加购。
- 无法唯一确定目标店输出源时，显示待选项，不按相似名称自动绑定。

现有配置级 ERP/核算价格输入与粘贴入口应统一调整：ERP 成本通过共享采集入口维护；人工核算价经差异预览写入共享成本源。不能继续静默产生只在当前配置生效的单价。尚未绑定真实 ERP ID 的行标为待绑定，不能以名称参与跨店成本同步。

## 3. 工作台操作设计

保留现有三栏：左侧商品导航、中间配置编辑、右侧图片预览。

顶部店铺选择器显示完整店名。共享入口标为“ERP / 库存同步 · 三店共用”和“核算成本 · 三店共用”；店铺入口标为“本店输出源”“本店模板”“本店图片设计”。不要用界面主题颜色作为唯一的店铺区分方式。

预览区提供两个独立页签：配置清单图、1:1 SKU 图。每个页签独立选择模板、编辑文字样式和素材位置，可并排检查两种图。

搜索、勾选、批量替换配件、价格导入、样式应用、删除和导出默认只针对当前店铺。跨店复用采用明确的“复制到其他店铺”动作。切店清空勾选并处理未保存草稿；异步保存与生成任务携带发起时的店铺身份，不能依赖完成时当前选择的店铺。

## 4. 三店图片设计方向

技嘉深色版黑橙、乔思伯黑白为用户已确认的设计方向；以下具体色值与排版为样稿建议，不是官方 CI 色值。实际 Logo 应使用核实后的正式素材，不用普通文字仿制。

| 店铺 | 配置清单图 | 1:1 SKU 图 | 拟定视觉特征 |
|---|---|---|---|
| 英特尔官方旗舰店 | 保留已认可的清晰蓝白清单，重整标题和信息层级 | 放大配置编号、CPU/显卡差异，配件紧凑排列 | 蓝白、规整清单；旧模板保留 |
| 技嘉旗舰店 | GIGABYTE 品牌区、黑底深灰参数行、橙色分类与重点，明确 CPU/显卡/主板层级 | 橙色配置编号，中部机箱与核心参数分栏，下方黑底白字配置摘要 | 深色版黑橙，白字保证型号可读，小圆角或直线分隔，突出硬件性能 |
| 乔思伯官方旗舰店 | JONSBO 品牌区与整机展示，黑白配件清单，以灰阶区分层级 | 上部机箱与核心配置，下部黑白清单，强调机箱型号与颜色 | 黑白，灰阶辅助，减少装饰，突出机箱和材质 |

拟定样稿色板：

- 英特尔：画布 `#FFFFFF`、正文 `#132B50`、强调 `#0070DC`、配件底色 `#EFF6FD`、分隔 `#DEEBF7`，延续现有工具配色。
- 技嘉深色版：画布 `#101010`、配件面板 `#1D1D1D`、正文 `#FFFFFF`、强调橙 `#FF7900`、辅助文字 `#B8B8B8`、分隔 `#353535`。橙色用于编号、分类、重点参数和分隔，不用于整段型号正文。技嘉浅色版尚未确定，本轮优先制作黑橙深色样稿。
- 乔思伯浅色版：画布 `#FFFFFF`、正文 `#161616`、面板 `#F2F2F2`、辅助文字 `#707070`、分隔 `#D9D9D9`；深色版：画布 `#111111`、正文 `#FFFFFF`、面板 `#202020`、辅助文字 `#BDBDBD`、分隔 `#383838`。两版均保持黑白与中性灰阶，设计元素不增加彩色强调；真实机箱照片保留原始颜色。

中文标题使用 Microsoft YaHei Bold，正文使用 Microsoft YaHei；型号和参数可用 Segoe UI，字体变更须验证实际环境可用。品牌辨识依靠正式 Logo、真实整机素材和版式，不依靠任意装饰或捏造性能数字。

GIGABYTE 与 AORUS 应区分：AORUS 是技嘉旗下高端电竞品牌，技嘉旗舰店基础模板以 GIGABYTE 为主；只有商品系列需要时增加 AORUS 对应模板，不默认整店套用 AORUS 标识。用户指定的黑橙配色不改变店铺名称或品牌标识。

乔思伯官方对机箱产品强调制造工艺与桌面家居美感，因此本方案建议以真实整机外观和材质展示为重点；这属于设计推导，不是对官方图文模板的复制。

参考来源（2026-09-16 查询）：[GIGABYTE 官网](https://www.gigabyte.com/tw)、[AORUS 官方介绍](https://www.gigabyte.com/tw/mb/aorus/about)、[JONSBO 官方产品介绍](https://www.jonsbo.com/en/)。技嘉品牌规范页面本次访问超时，未核实正式 CI 色值与 Logo 使用细则；上面的色板只作为设计草案。

## 5. 两种图的独立排版规则

### 配置清单图

默认宽 1400px；常规高度沿用 1834px，内容多时自然增高。结构为品牌与配置身份、整机摘要、完整配件清单、升级/加购、店铺服务与说明。型号、数量、升级价等来自结构化数据。

### 1:1 SKU 图

固定 1400 × 1400。初稿分区：顶部约 12% 显示品牌与配置编号，中上部约 24% 显示机箱和核心配置，下部约 56% 显示配件清单，底部约 8% 放简短服务或说明；这些比例用于样稿起点，可按店铺模板调整。

方图默认保留已选择展示的配件信息。名称可用专门维护的展示简称，但不得丢失型号、容量、数量、显卡是否包含等购买差异。内存总容量的摘要需要明确规格字段支持，不能从模糊商品名猜算。

内容超出时，先减少空白与非必要营销文字，再调整机箱区占比和行距。达到预设可读字号下限仍放不下时，提示具体溢出模块并阻止该图正式导出，不自动删配件或裁掉内容。样稿同时检查原尺寸和约 350px 宽预览。

店铺模板内两种图分别保存布局、模块顺序、展示开关、字体、字号、配色、机箱位置和缩放；共用真实配置数据。客户可见的业务文字编辑回写结构化字段，纯排版调整不改业务数据。

两种图均使用真实机箱素材并保持比例；素材照片中出现的硬件不自动视为该配置包含的硬件。成本、利润不进入客户图片；售价不默认新增到图中。

## 6. 模板与版本设计

每店至少两个独立模板：`config_detail`、`sku_square`；浅色/深色作为模板下的主题变体。

建议关系：

```text
共享配件成本源(goodsId)
  ├─ 英特尔输出源 → 英特尔商品配置 → 清单模板 / 方图模板
  ├─ 技嘉输出源   → 技嘉商品配置   → 清单模板 / 方图模板
  └─ 乔思伯输出源 → 乔思伯商品配置 → 清单模板 / 方图模板
```

模板继承：店铺默认 → 商品链接指定模板 → 单套配置局部样式。已保存的旧图保留模板版本；发布新模板后列出差异和受影响配置，选择范围再应用，不在打开旧配置时自动覆盖设计。

生成记录至少包含 shopId、productId、configId、图片类型、配置内容版本、模板版本、素材版本和生成状态。重绘依据图片实际依赖：仅成本更新且图中不展示成本时只重算，不批量重绘；配件、名称、加购或素材变化才更新对应图片。失败显示失败或旧图，不能标成已同步。

导出按 `店铺/商品链接/配置清单图`、`店铺/商品链接/SKU图` 分目录，保留现有图片命名习惯，增加 manifest 记录真实 SKU ID、内部配置 ID、模板与配置版本及失败原因。允许保留缺少真实 SKU ID 的草稿状态，不能用配置编号冒充平台 SKU ID。

## 7. 对现有代码的改造落点

| 现有文件 | 改造内容 |
|---|---|
| prototype/index.html、app.js | 店铺切换、当前店范围、独立两种图编辑、共享与本店入口；移除单店硬编码 |
| prototype/core.js | shopId 归属、跨店复制规则、模板与字段继承，保留真实配件数量和 ERP 输出 |
| prototype/source.js、source-editor.js | 店铺展示层与共享成本层拆分；店铺字段差异同步不得跨店 |
| prototype/erp-sync.js、cost-editor.js | 共享同步入口与影响范围；ERP 更新保留人工核算价和店铺文案 |
| prototype/poster.js、poster-editor.js | 按店铺模板及图片类型渲染，保留文字命中编辑与溢出检测 |
| prototype/global-batch.js、batch-appearance.js、price-import.js | 默认本店范围，批量导出调用两种独立模板 |
| prototype/server.mjs | 服务端检查店铺归属、共享成本写入、模板版本与导出记录；禁止整份旧草稿覆盖新共享成本 |

当前后端每次提交整份状态且上限为 500 套配置。第一阶段可继续本地文件存储，但须审查三店容量与写入契约，保留版本冲突检测并拆分共享成本和店铺内容写入；不能只增加前端下拉框。未来多用户部署另行引入账户和权限，不能把本地 shopId 过滤称作权限系统。

## 8. 迁移、实施与验收

1. 对现有数据和素材做完整备份，在隔离副本演练；既有配置、模板及样式归入英特尔店，保留全部 ID、数量、售价与图片文件。
2. 核对共享成本源与配置快照差异。相同 goodsId 的旧核算价冲突列出待处理，不能静默采用最后一条或未验证的最高/最低值。
3. 建立另外两店空商品空间和本店输出源；可从现有输出源生成独立初稿，不自动复制商品、平台 SKU、加购或服务承诺。原图与旧模板仍可查看。
4. 完成店铺切换、共享成本、独立名称/加购，再设计技嘉和乔思伯四张样稿；英特尔旧图保留，同时制作独立方图新版。三店样稿使用同一真实配置比较，覆盖无卡、长型号、多数量和附加配件。
5. 接入模板选择、双图预览、批量生成与导出，最后迁移日常实例。

关键验收：

- 同一 goodsId 三店共享成本与库存；三套不同名称/加购在 ERP、核算更新后仍各自保留。
- 成本按真实数量重算，ERP 同步不覆盖人工核算单价；同数量同配件成本一致，不同店售价与利润可不同。
- 技嘉店批量改名、改加购、改样式，不影响另两店；清空加购后同步不会复活。
- 跨店复制清空来源平台身份和店铺营销内容，绑定目标店的输出源和当前共享成本。
- 两种图独立改样式、读同一份业务配置；方图严格 1:1，长型号和多配件不截断，预览与导出一致。
- 三店同名链接与配置导出不混淆，模板更新不改旧图；失败清单与成功文件数量可核对。
- 旧页草稿、撤销、备份恢复不能静默回滚后来更新的共享成本；切店后的旧图片任务不能覆盖当前店预览。
- 现有维护表复制列数、顺序、长 ID 文本和数量保持；迁移前后数量、关键字段及素材引用逐项核对。

2026-09-16 样稿更新：用户要求按照英特尔当前样式制作，因此本轮优先沿用现有布局，仅替换店铺品牌文字及配色，上文分栏等新布局暂不实施。已在 `设计例图/三店样稿V1/` 制作技嘉黑橙、乔思伯黑白各一张配置清单图和一张 SKU 方图，并检查尺寸、溢出与配置数据一致性。用户随后回复“可以”，四张样稿设计已确认，作为后续模板接入的设计基准。三店功能尚未接入，业务代码、正式数据和运行中的服务未改动。
