# 本地仓库说明

配置工具根目录是当前主仓库，沿用原公开仓库的提交历史。当前源码采用 Python + WebView2 桌面端；`prototype/` 仍是桌面端与轻量网页版共用的前端和业务规则，不能整目录删除。

当前功能和部署入口统一见 [README](README.md)。旧发布副本 `github-public/` 已在 2026-09-23 移入回收站；清理前确认工作区干净，全部提交已包含在根仓库中。

## 开发与依赖

Python 环境由 `python-desktop/pyproject.toml`、`uv.lock`、`.python-version` 管理；开发与构建命令见 [Python 桌面说明](python-desktop/README.md)。前端仍通过 `prototype/package.json` 和 `package-lock.json` 管理 Node 依赖。

旧 Electron 打包、主进程和启动入口已退役；`electron-builder` 已退出依赖。`electron` 暂保留，用于 Node 凭据模块读取旧格式；Python 中的旧凭据迁移和兼容测试继续保留。`python-desktop/tests/legacy-credentials.mjs` 是可选历史验收，需要保留的旧 Electron EXE，不属于默认测试。

## 本地数据

`prototype/seed.json`、`prototype/catalog.json` 是本地业务文件，由 Git 忽略；版本库提供空白的 `.example.json` 文件。

新克隆目录在启动 Node 原型前，需要生成本地数据文件；已有文件不覆盖：

```powershell
if (!(Test-Path prototype/seed.json)) { Copy-Item prototype/seed.example.json prototype/seed.json }
if (!(Test-Path prototype/catalog.json)) { Copy-Item prototype/catalog.example.json prototype/catalog.json }
```

Python 构建自行生成空白 seed/catalog，不复制本地业务文件。默认 Node 单元测试使用合成夹具，不读取上述本地文件。

`.venv/`、`node_modules/`、生产环境配置和独立开发数据保留本地并由 Git 忽略。清理记录和生产部署记录属于本机维护资料，不是新克隆项目的运行依赖。

## 版本、提交与发布

当前版本和使用入口见 [README](README.md)，功能变化见 [更新日志](RELEASE_NOTES.md)。Git 远程 `gitea` 指向公司 Gitea 仓库，`origin` 指向 GitHub；以 `git remote -v` 的实际输出为准。

更新前运行 `git status --short` 和 `git fetch <远程>`，核对本机改动与远端提交。需要拉取时使用 `git pull --ff-only <远程> main`；出现分叉先处理差异。推送主分支后，用 `git ls-remote <远程> refs/heads/main` 与本机 `git rev-parse HEAD` 核对提交。

Git 推送只更新源码引用，Release 标题、说明和附件需要在对应平台另行维护。发布 Windows 包时执行 [构建与打包](docs/部署与开发.md#测试与打包)，核对版本、空白业务种子、示例云配置和 SHA256 清单，再上传对应版本的附件。Gitea 和 GitHub 的附件不随 Git 自动同步。
