# 在线配置表样例分析

核验日期：2026-09-14。来源：[英特尔 主机 配置 在线 XLSX](<C:/Users/d1832/Downloads/英特尔 主机 配置[在线].xlsx>)。

本文件为用户删减后的真实样例，用来确定输入、公式和输出格式，不能作为全店商品清单或完整迁移数据。原文件只读，未修改、重算或回写；分析读取了 OOXML 中的单元格、公式、共享公式、格式和缓存结果，关键汇总另按 Decimal 加总核对。没有执行 Excel/WPS 原生重算，也没有访问工作簿中的外部链接。

SHA-256：`e44e999e0d9bc2d341d914a66d7f3f45c4f1d92b7556a2311670c4ba8d7a8119`。

## 1 可用于首轮开发的样例

共保留 26 个工作表。其中 7 个工作表名称带商品 ID，可以作为商品链接候选；名称不能单独证明店铺身份或平台当前状态。

| 层次 | 工作表及作用 |
|---|---|
| 成本输入 | `成本源`：ERP ID、商品名称、可销数、ERP 单价、含税单价，双层表头 |
| 配件主数据 | `输出源`：ERP 型号、展示名称、成本引用、库存、质保、升级文案、容量等 |
| 名称辅助规则 | `输出源 勿删 勿动`：CPU/GPU/显存等名称与规格匹配词表 |
| 配置模板与框架 | `配置模板`、`主机模板 复制用 勿动 `、黑白 ATX/MATX、无卡与机箱配置例子 |
| 分组页 | `配置框架`、`日常主推链接`在此文件中为空，不创建为空商品 |
| 商品实例 | 以下 7 张带商品 ID 的工作表 |

| 商品工作表 | 带 CPU 文本的配置块候选 | 预留标题块总数 |
|---|---:|---:|
| X400白3797170100491124774 | 20 | 20 |
| X400 3795706371341680953 | 20 | 40 |
| S960V2 3812171617979728156 | 15 | 40 |
| 680清仓3825219157570486339 | 5 | 40 |
| S900新3797202651888877621 | 10 | 40 |
| 包豪斯新3801278326744678775 | 16 | 40 |
| 270K+5060TI 3797540741002166472 | 8 | 40 |

合计 94 个带 CPU 文本的候选块，尚未逐个完成配件匹配和业务有效性确认，不等于 94 个已经验收的销售 SKU。不能按预留“配置1—配置40”标题数量直接创建 SKU。

首轮建议采用 `X400白3797170100491124774`，它的商品 ID 与此前提供的参考链接相同；前两个配置位于 `A1:N19` 和 `A20:N38`，可用于图文联动和多数量验收。该工作簿没有 `xl/media` 图片文件，不能从中取得商品详情配置图模板。

## 2 来源字段和查找链

`成本源!A1:E2` 两层表头为：

| 列 | 中文/业务表头 | 内部字段 | 系统含义 |
|---|---|---|---|
| A | goods_id | goods_id | ERP 配件 ID，字符串处理 |
| B | 商品名称 | b_c_name | ERP 原始名称 |
| C | 可销数 | n_able | 库存来源值 |
| D | erp成本 | d_cost | ERP 单件成本 |
| E | 含税成本 | 无内部表头 | 人工同步的实际核算单件价 |

`成本源`保留 16,099 条非空 ID 数据行，未发现重复 ID。`I1`是更新时间标签，`J1`存日期值 46279，即 2026-09-14，显示格式为月日；它不是脚本成功执行的时分秒证据。工作台仍需单独记录采集任务的实际成功时间。

`输出源`中 A=ID、B=ERP 型号、C=ERP 成本、D=含税成本、G=展示名称、H=可销数、J/K=质保信息、L=升级文案、R/S/T=显存/内存/硬盘容量。保留 1,193 条非空 ID 行，对应 1,192 个不同 ID；其中重复项需辨别展示变体，不直接删除。

已跟踪的路径：`成本源 → 输出源 → 商品配置块`。例如 `输出源!C23`按 A23 的 ID 查成本源 D 列；`输出源!D23`当前按 B23 的 ERP 名称查成本源 E 列；商品配置 `A3/B3`再按 H3 的展示名查输出源 C/D 列，并乘 E3 数量。

新系统建议以稳定 ID 关联成本。旧样例中按名称查找的路径仍需记录和对比，不能默认名称唯一，也不能把不同名字或不同 ID 的记录自动合并。

## 3 配置块结构

以下以 X400 白链接为依据。配置标题行依次为 1、20、39、58……362，即间隔 19 行。最后一个块可缺末尾分隔空行，解析器不应因不足完整 19 行就丢弃有内容的尾块。

| 区域 | 实际含义 | 实施要求 |
|---|---|---|
| 每块第 1 行 | ERP/含税总价、核算规则、利润、到手价标签；K 为配置编号，L 为版本展示名 | 与明细行的相同列分开解释 |
| 每块第 2 行 | 成本汇总、利润结果和销售价格 | 读取公式与计算值，不能作为配件行 |
| A/B 明细 | ERP/含税行成本，已经乘过数量 | 不再乘第二次数量 |
| C/D/E 明细 | 可销数、ERP ID、数量 | 本例 E 列是数量，F 为空；其他模板 F 有辅助值也不能取代 E |
| G/H | 配件分类与基础展示名称 | 保留 CPU、散热、主板、内存、硬盘、显卡、电源、机箱、风扇及其他配件 |
| K/L | 配件分类与展示文本 | L 多处是 H 加数量后缀的公式，不因列标题为“进阶版”就自动创建另一套独立 SKU |
| M/N | 质保和升级文案 | 文案是原始展示数据，不能仅凭一句升级说明推断目标 ERP ID |
| O/P | 配置索引和跳转展示内容 | 属于导航信息，不重复创建配置 |
| Q:T | SKU 简称、CPU、GPU、显存 | 保留旧命名规则；部分其他模板延伸到 U/V 计算内存与硬盘容量 |
| AA:AW | 多组 ID/数量辅助输出区域 | 存在引用错位，不能直接当作已正确的维护表复制契约 |

无卡例子：`H8`为“不含显卡，可咨询客服加装独显使用”，`D8`为 0、成本为 0；这是明确的无独显占位语义，不应伪造配件 ID。空配件行则应结合名称与分类判断，不因为公式返回 0 就创建真实配件。

`G13`标签为“风扇”，但 H13 是电源延长线。系统保留原行标签以便溯源，标准分类需匹配配件库或人工确认；不能只用固定行标签认定实际品类。

## 4 已确认的计算规则

`X400白3797170100491124774!A2:G2`：

| 项目 | 文件中的公式 | 系统表达 |
|---|---|---|
| ERP 总价 | `SUM(A3:A17)` | 各有效 ERP 行成本之和 |
| 含税总价 | `SUM(B3:B17)` | 各有效含税行成本之和 |
| 4%+100 | `B2*1.04+100` | 含税总成本 × 1.04 + 100 |
| EXCEL 利润 | `G2-C2` | 到手价 − 上述含税核算基数 |
| ERP 利润 | `G2*0.98-A2` | 到手价 × 0.98 − ERP 总成本 |

本链接 20 个配置块的 C/D/E 汇总公式均采用相同的相对关系。该规则已从文件确认，不再仅凭截图猜测。4%、2% 与固定 100 的业务解释仍不据名称推断为税率或平台费；迁移中保留规则参数与来源。

首配置存储值：ERP 总成本 6520，含税总成本 6440，到手价 6699。独立计算得到含税核算基数 6797.60、EXCEL 利润 -98.60、ERP 利润 45.02，与缓存结果在浮点误差范围内一致。

数量验证：`输出源!D559=700`，本配置 `E6=2`、`B6=1400`；`输出源!D1115=28`，`E12=8`、`B12=224`。源数据是单件价，配置明细是乘数量后的行成本，与用户确认一致。

金额单元格使用 `0_ ` 的整数显示格式，公式本身没有 ROUND。比如 C2 存 6797.6，不能因为界面显示整数就先取整再算利润。新系统使用 Decimal 计算；显示精度与业务舍入分别配置。

旧 `配置模板!E1`采用 `B1*1.02+100`。导入时保留为不同规则版本，不直接统一为 4%；新建配置的默认规则以当前主机模板及商品样例为依据，若需淘汰旧规则再统一调整。

对 7 张商品表的 94 个候选块，使用现存明细缓存做了 188 个 ERP/含税汇总核对：132 个可数值加总的结果与顶部缓存相符，56 个因错误值不可核对。这个检查不证明底层名称匹配全部正确，也不等于原生重算通过。

## 5 样例中需要保留的异常

| 位置 | 发现 | 迁移处理 |
|---|---|---|
| 成本源 E7569 | 存储值为 `#VALUE!`，不是有效含税单价 | 显示缺失/异常，不替换成 0，不传播为正常报价 |
| 输出源 A436/A437 | 同一个 ID 250682 对应两种展示名称 | 标记可能的展示变体，人工辨别；不静默丢行 |
| 输出源 G504/G505 | 相同展示名称对应 ID 261527 和 261520 | 名称匹配有歧义，必须用 ID 或人工确认 |
| X400 白 AA6/AB6 | `AA6=D9` 指向电源，但 `AB6=E6` 指向内存数量 2；电源实际 E9=1 | 辅助输出有 ID/数量错位，V5 默认解析不使用此区，而读取配件行 D/E；新输出从同一配件关系取 ID 和数量 |
| X400 白 AA7/AB7 | 指向风扇 D12，但数量公式为 E7=1，实际风扇 E12=8 | 同上，不能直接复制辅助区域当作正确结果 |
| 工作簿整体 | 共 314 个已保存的 `#VALUE!`/`#N/A` 单元格 | 按单元格保留异常清单；未断言全部由删减造成，也未据此断言线上表同样异常 |
| 输出源工作表范围 | 标记范围延伸到第 1,047,771 行，但只有 1,301 行有值或公式 | 导入按实际内容扫描，不能把样式/空范围当作百万条业务记录 |

本次不修复原文件。原生共享公式已针对 AA/AB 关键位置展开确认，所以错位发现不是把共享公式子单元格误判为空公式造成的。

## 6 对待办的影响

已取得真实 XLSX 样例、定位参考链接对应工作表、确认 19 行结构及当前利润计算、确认单价乘数量规则，并发现字段与辅助输出的迁移边界。这些是 S0-01、S0-02、S0-04、S0-05 的部分证据。

已取得框架勾选工具及粘贴工具 V5.0 源码，已核验默认横向输出纯函数；尚缺真实配置的浏览器复制结果、主机维护表实际目标粘贴区域、配置图或源模板、ERP 采集脚本。不能把整个 S0 或任何完整实现任务勾选完成；当前进度仍为 0/38。

当前工具契约见[现有流程与工作台衔接](D:/工作/PythonProject/配置工具/现有流程与工作台衔接.md)：每套 56 列无表头 TSV，包含名称查找公式。下一步用本文件中 X400 白的前两套配置建立最小在线编辑、图文联动与复制验收样例。复制区必须按真实目标表确定；不是因为已收到来源 XLSX，就认定已确认另一个目标维护表的全部格式。
