# 本机桌面启动

项目目录中的“启动桌面工作台.cmd”现已改为启动 Python 桌面版。启动器选择 release/python-* 目录内最新构建的程序。原快捷方式如果指向此 CMD，也会使用 Python 版；直接指向旧 EXE 的快捷方式仍运行旧版。

Python 版使用完整文件夹分发，EXE 与 `_internal` 放在一起运行，不再每次解压 Electron 到 TEMP，也不要求其他电脑有 D 盘。启动器不修改 TEMP/TMP。

已经运行的旧版需要正常关闭后再从新入口启动。SQL、云同步、SQLite 与文件存储由 Python 后台负责，现有网页界面由 Windows WebView2 显示。其他电脑请完整解压对应版本的 Python 发布 ZIP。

0.3.1 起业务数据默认保存在 EXE 同级的 `data` 文件夹，登录缓存与运行信息在 `data/runtime`。新工具文件夹没有 data 时会复制迁入旧 AppData 数据，原目录保留；已有 data 不覆盖。以后升级或移动程序请保留 data。首次启动 Python 版前自动备份原状态、同步库和历史库；首次需重新登录。详情见 [Python 使用说明](python-desktop/使用说明.txt)。旧 Electron 启动与构建入口已从当前源码移除；如需回退旧版，可从 Git 历史取回源码，已有旧发布包仍保留。
