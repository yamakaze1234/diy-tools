# 现有流程与工作台衔接

更新日期：2026-09-14。以用户当前描述和提供的两份 HTML 源码为准；V5.0 是现行粘贴工具，需求书中的 V2.0 不再作为当前版本。仅分析流程和输出，没有修改原工具或在线表格。

## 当前实际流程

1. 在「框架配置勾选工具」保存配置模板，勾选要使用的现成配置。
2. 复制所选配置，到主机模板的 F 列起始位置粘贴 F:H 三列内容。
3. 在在线表格中补充、更换配件和数量，得到完整配置及对应 ID 等信息。
4. 把完整配置复制到「配置粘贴工具 V5.0」，生成「ERP 导入—横向」。
5. 复制 ERP 横向结果，粘贴到主机维护表。

这里的“ERP 横向”是维护表当前接收的格式，不代表工具已经写入 ERP 系统。用户尚未说明现有图片生产的完整操作步骤，不能把未来图片自动生成当作当前已实现流程。

## 源码核实的输入与输出

| 环节 | 当前规则 | 工作台需保留的行为 |
|---|---|---|
| 保存框架模板 | 浏览器 localStorage 保存名称、时间和原始 sourceText；同名替换 | 模板迁到共享数据库，保留来源及版本，多人可调用 |
| 勾选调用 | 每套 19 行、每行 3 列；按源列表顺序拼接，保留空行；不足 19 行的尾块补齐 | 首先支持勾选现成配置，生成可独立编辑的商品配置草稿 |
| 中间粘贴 | F:H 文本，从目标 F 列开始粘贴；工具不生成新的查找公式 | 工作台内部直接传递字段，无需中间复制粘贴；迁移期可保留旧格式导出 |
| V5 配置解析 | 每 19 行一套，最多读取 30 套；D=ID、E=数量，K/L 优先作为类型/展示名，空时回退 G/H；M/N 为质保/升级 | 按真实行结构导入并保留异常；超出限制明确提示，不能静默遗漏 |
| ERP 横向 | 每套一行、14 个配件槽位，每槽 4 列，共 56 列 | 固定列序，支持单套和多套，不增加会挤偏列位置的版本/配置名列 |
| 复制结果 | 无表头 TSV，列间制表符、行间 CRLF | “复制到主机维护表”直接复用此格式 |

14 个槽位依次为：CPU、散热、主板、内存、硬盘、显卡、电源、机箱、风扇、配件1、配件2、配件3、配件4、配件5。每槽列序固定为 `配件_id → 配件名称公式 → 配件_数量 → 配件_备注`。

实际复制会将空 ID 输出为 `0`，空数量输出为 `1`，备注为空，名称列仍保留公式。这是旧格式兼容规则，不表示空槽位真实配置了一件配件。工作台内部应保存真实的空配件语义，只在兼容导出时转换。

名称公式示例（CPU）：

```excel
=IF([@[CPU_id]]<>"",INDEX([主机成本表V2.xlsb]库存数据!$BD:$BD,MATCH([@[CPU_id]],[主机成本表V2.xlsb]库存数据!$BB:$BB,0)),"")
```

公式依赖目标区域的结构化表头及外部工作簿，不能替换成展示名称后声称完全兼容。用户暂不提供宏工作簿副本；依据截图和源码推进，外部引用和 ID=0 时的实际计算结果由用户本机试用验证，不作为原型前置。

V5 的通用复制按钮在选中列时可能仅复制该列；专用全量复制输出完整横向表。新工作台的“复制到主机维护表”应固定复制全量选定配置，不受预览选中列影响。工具还有按版本设置风扇/配件来源、数量和直接 ID 的逻辑，后续要用用户实际设置样例核对，不能只迁移默认分支。

源码位置：勾选工具 `ToolCore`（第 242 行起）、`currentSelection`（第 522 行）；V5 `buildFromRows`（第 1802 行）、`erpPartForSlot`（第 3992 行）、`erpFormulaFor`（第 4032 行）、`buildErpTables`（第 4037 行）、`erpRowsToTsv`（第 4087 行）。源码及 SHA-256、合成输出证据见[输出契约核验](D:/工作/PythonProject/配置工具/现有工具输出契约核验.json)。

## 工作台整合后的流程

选择店铺和商品链接 → 勾选共享模板中的配置 → 在线补充或批量修改配件 → 保存配置版本。

保存后的同一份配置供两个出口使用：

- 配置清单图：编辑时即时预览，保存后自动生成并保存对应版本图片，批量导出后人工上传。
- 主机维护表：按同一配置顺序提供 SPU、SKU ID、简称列与 V5 兼容的 56 列配件横向内容，用户分区域粘贴到原维护表。

每日成本更新放在工作台中统一处理：ERP 脚本一键更新并记录成功时间；人工粘贴含税单价，按数量重新核算。内部成本不默认进入客户配置图。

多人协作需要共享模板与配置存储、独立账号和店铺权限、更新通知及保存版本校验。同一配置被他人修改时提示差异，不能静默覆盖；图片和复制输出均追溯到已保存配置版本。

现有勾选工具选择的是完整配置块，不能把它描述成已有 CPU×显卡自动组合功能。需求书中的组合生成保留为后续能力；首轮先覆盖用户已经在用的模板选取、补配和最终复制。

## 核验范围及剩余工作

已只读检查两份源码；提取 V5 输出纯函数，以两套合成配置运行，确认每行 56 列、无表头、多数量保留，以及空槽位的 0/1/空备注转换。该验证未运行完整浏览器页面、剪贴板或 Excel/WPS，也未证明所有真实配置解析正确。

接下来用至少三套真实配置覆盖独显/无卡、多数量及配件槽位设置，对比 V5 实际结果；按截图核对目标区域，保留用户本机试用验收；用已提供详情图和真实机箱素材建立字段绑定。XLSX 的 AA/AB 辅助区不作为复制结果依据：V5 默认解析直接从同一配件行读取 D/E，不使用该辅助区。来源类型误标或重复仍可能影响槽位，需保留异常处理。


### 主机维护表与真实素材补充（2026-09-14，当前有效）

用户确认维护表人工维护范围为 SPU、数字 SKU ID、简称和展开的配件列。用户已明确：原表带宏且数据量大，暂不提供副本。不再把取得维护表副本作为需求整理、原型或功能开发的前置；不读取、修改或执行该宏工作簿。依据提供的截图和 V5 源码建立输出样例，由用户在本机试用时确认实际粘贴结果，未完成该步骤不得宣称宏表联调通过。

输出应支持复制 SPU 列、SKU ID 列、简称列和配件横向区域；这些输出共享同一配置集合及顺序，逐列行数一致。支持批量粘贴/维护 SPU 和真实 SKU ID，以字符串保存；空值明确标记，不能用配置序号、名称或随机值代替真实 ID。截图可见 SPU 位于 G、SKU 位于 H、简称位于 O、配件区域自 P 起；中间有隐藏/辅助列，所以不能直接把三个字段与 56 列拼成连续 59 列后声称可整行粘贴。若以后增加整行复制，应先明确完整物理列映射；当前保留按目标区域一键复制的方式。

源码证据：V5 的 ERP导入-配置简称支持两种名称格式及无表头复制（1770—1792、4837—4843 行），展开配件为无表头 56 列 TSV（4037—4092、4871—4880 行）；SPU 在记录中输入/保存。当前提供的源码尚未核实独立数字 SKU ID 的复制入口，pkg.sku/Canva 的 SKU简称不能据名称当作平台 SKU ID。用户现有操作作为兼容目标保留，原型补齐明确的 SPU/SKU ID 输入及复制入口，后续用实际操作核对，当前不要求再次提供工作簿。

已收到真实机箱素材：`D:/工作/主机详情页/X400 CG/白色/A-主图/DSC05649-已增强-NR.png`，2278×2277、RGBA，存在透明像素。原型按 1:1 素材位等比完整放置，1 像素尺寸差通过容器留白处理，不拉伸或裁切原图。它是机箱展示素材，不能从照片推定实际配置清单中含有所有可见硬件。
